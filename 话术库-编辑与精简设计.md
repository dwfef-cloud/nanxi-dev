# 话术库改造设计（删话术策略 + 可编辑 + 卡片精简）

日期：2026-09-13　状态：待确认后实施

---

## 一、目标

1. **删掉「话术策略」模块**，话术只留在「话术库」一处。
2. **话术库支持真正编辑**：现在只能「用画像解析预览」，改不了内容。
3. **话术卡片缩小**：现在一张卡 454~613px，一屏看不了几条。

---

## 二、删掉「话术策略」

### 现在是什么
业务画像页第 4 张卡「话术策略」，内容不是表单，而是：
- 5 类场景就绪检查（评论区首次触达 / 欢迎语首句 / 私信开场 / 加微引导 / 异议应对）
- 「按业务画像生成缺失的 N 类话术」按钮
- 「导入旧版话术」按钮（老数据一次性迁移）
- 「去话术库编辑」链接

### 怎么删
| 项 | 处理 |
|---|---|
| 业务画像页第 4 卡 | 整张删除，页面上从 5 个模块变 **4 个**：业务画像 / 产品知识库 / 目标客户 / 微信转化，编号 1~4 |
| 前端代码 | 删 `scriptsCardHtml` / `loadBizScripts` / `SCENE_CATS` / `LEGACY_SCRIPT_MAP` / `bsc-*` 样式 |
| 后端 `onboarding.get_status` | `total` 5 → 4，`steps` 去掉 `scripts`（否则永远差一步，状态永远完成不了） |
| 话术库页 | 顶部新增一行「场景就绪」提示条 + 两个按钮（生成缺失话术 / 导入旧版话术） |

### 功能不能丢，去哪
- 「按画像生成缺失话术」→ 话术库页顶部按钮（AI 生成，约 10~60s）
- 「导入旧版话术」→ 话术库页顶部（有旧数据才显示）
- 5 类场景就绪状态 → 话术库分类 tab 上本来就有每类条数，顶部提示条只说一句「5 类场景已就绪 4/5」

---

## 三、话术库可编辑

### 缺什么（后端）
| 能力 | 现状 | 要补 |
|---|---|---|
| 改名称 / 分类 / 启用停用 | ✅ `PATCH /api/scripts/{id}` | — |
| 改备注 intro、企微欢迎语 | ❌ Schema 里没这两个字段 | `ScriptUpdate` 补 `intro` / `welcome_msg` |
| 改变体文本 / 权重 / 状态 | ✅ `PATCH /api/scripts/{id}/variants/{vid}` | — |
| 加变体 | ✅ `POST /api/scripts/{id}/variants` | — |
| 删变体 | ❌ | `DELETE /api/scripts/{id}/variants/{vid}` |
| 删整条话术 | ❌ | `DELETE /api/scripts/{id}`（连带删变体） |

### 编辑弹窗（宽版，和业务画像弹窗同款）
点卡片上的「编辑」打开：

```
┌ 编辑话术 ──────────────────────────────────┐
│ 话术名称 [____________]   分类 [下拉]       │
│ 备注（选填）[__________________________]   │
│ 企微欢迎语（加微后自动发送）[___________]   │
│ ☑ 启用（停用后评论/私信不会再取这条）      │
├ 变体 ─────────────────────────────────────┤
│ ┌ A  权重[40]%  状态[使用中▾]        [删除] │
│ │ [变量芯片：{行业} {产品} {卖点} …]        │
│ │ [话术文本 textarea               ]       │
│ │ 预览：按当前画像解析 → …                  │
│ └─────────────────────────────────────────┘
│ ┌ B …                                      │
│ [+ 添加变体]                                │
├───────────────────────────────────────────┤
│ [删除整条话术]        [取消]  [保存]        │
└───────────────────────────────────────────┘
```

要点：
- 变量芯片点击插入到**光标处**（和现在新建弹窗一致）
- 每个变体下面有「按当前画像解析预览」，实时
- 权重边上给一句提示：`权重是 A/B 分配比例，建议合计 100%`
- 保存 = 一次性提交（改话术 + 逐个变体改/增/删），失败给明确提示
- 删除变体/整条话术要二次确认

---

## 四、话术卡片精简

### 现状（1440 宽实测）
| 指标 | 实测 |
|---|---|
| 卡片尺寸 | 373 × **454~613px** |
| 变体行高 | **113~135px**（每个） |
| 企微欢迎语块 | 6/7 张卡是**空的仍占 24px** |
| 卡片头 / 底部按钮 | 24px / 28px |

结论：高度几乎全是变体行堆出来的，另外空欢迎语白占位置。

### 目标
单卡高 ≈ **180~220px**（比现在降 60%），一屏能看到 6~9 条。

### 做法
1. **变体行 135px → 约 44px**
   - 变体字母 A/B/C + 状态 + 权重挤在一行（当前是竖排大块）
   - 文本**默认 1 行截断**（`…`），点这一行展开全文；再点收起
   - 发送/回复/加微/转化率四个指标：默认只显示「转化率」，其余 hover 或展开后看
   - R2 提示改成行尾一个小标记 ⚑（hover 出原文）
2. **空的企微欢迎语不渲染**（有内容才显示一块）
3. **操作按钮移到卡片标题行右侧**：`编辑` `▾更多`（更多里放：用画像解析预览 / 停用 / 删除）
4. **分类 tab 与提示条合并成一行**，减少顶部占位
5. 网格保持 3 列（现在已是 3 列），窄屏自动降 2/1 列

---

## 五、落地清单（文件级）

**后端**
- `schemas/script.py`：`ScriptUpdate` 补 `intro` / `welcome_msg`
- `services/script_service.py`：`update_script` 支持这两个字段；新增 `delete_script` / `delete_variant`
- `repositories/base.py` + `sqlite.py` + `memory.py`：新增 `delete_script` / `delete_variant`
- `api/routes/scripts.py`：新增 2 个 DELETE 端点
- `services/onboarding_service.py`：`total` 5 → 4，`steps` 去掉 `scripts`

**前端**
- `app.js`
  - 删「话术策略」卡（`scriptsCardHtml` / `loadBizScripts` / `SCENE_CATS` / `LEGACY_SCRIPT_MAP` / `BIZ_STEPS` 第 4 项）
  - 业务画像页 5 → 4 卡、编号重排
  - `scriptCard()` 重写为精简版 + 行内展开
  - 新增 `openScriptEditModal(script)`（编辑弹窗）
  - 话术库页顶部加「场景就绪 + 生成缺失话术 + 导入旧版话术」
- `api-business.js`：补 `deleteScript` / `deleteScriptVariant`
- `style.css`：精简卡片样式 + 编辑弹窗样式；删 `bsc-*`
- `index.html`：版本号

---

## 六、待确认

1. 卡片精简力度（瘦身卡 / 折叠卡 / 列表行）
2. 编辑能力是否含「删除变体 + 删除整条话术」（要加 2 个后端 DELETE 接口）
3. 「按画像生成缺失话术」入口放哪（话术库顶部 / 直接去掉）
