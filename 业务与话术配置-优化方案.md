# 业务与话术配置 · 优化方案

> 2026-09-14 · 基于当前代码与真实库数据体检后编写，供你确认后再动手。

---

## 一、现状体检（用真实数据说话）

当前这一页 = 两个 tab：**业务画像**（4 张卡，共 25 个字段）+ **话术库**（12 条话术）。

真实库里现在是这样：

| 卡片 | 字段数 | 实际填了 |
|---|---|---|
| 业务画像 | 7 | 只有「转化目标=添加微信」「沟通语气=专业、真诚」（还是系统预置默认值） |
| 产品知识库 | 7 | 空 |
| 目标客户 | 7 | 空 |
| 微信转化 | 4 | 空 |

话术库 12 条，5 类场景都"就绪"，但其中 5 条是 **「· 通用」**（因为画像空，AI 只能生成通用话术）。

**一句话结论**：字段设计得挺全，但**重复、且填了不知道有什么用**，所以实际只用到了 2 个系统默认值就停下了。

---

## 二、问题清单

### P0-1　同一个概念要填 2~3 遍（字段重复）

| 概念 | 现在出现在哪 | 哪个真正生效 |
|---|---|---|
| 产品 | 业务画像「主营产品」 + 产品知识库「产品名称」 | **只有「产品名称」**（AI 和话术变量都优先取它，主营产品只是兜底） |
| 价格 | 业务画像「价格区间」 + 产品知识库「价格区间」 | **只有产品知识库那个** |
| 目标客户 | 业务画像「目标客户」（单选） + 产品知识库「目标客户群」（多选） | **只有业务画像那个**（AI 用它；多选那个只作变量） |
| 行业 | 业务画像「所属行业」 + 目标客户「所属行业」 | 两个都生效，但**字面完全一样、没有任何说明**，用户会以为是重复 |
| 区域 | 业务画像「服务区域」 + 目标客户「所在区域」 | 同上（一个是"我能服务到哪"，一个是"客户在哪"） |

前三条是**真重复**（填了白填）；后两条是**真不同但没说明**，容易被当成重复而漏填。

### P0-2　填了不进 AI、也不进话术（幽灵字段）

25 个字段里，AI 生成话术只用了 16 个。剩下 9 个的情况：

| 字段 | AI 用吗 | 话术变量有吗 | 真实用途 |
|---|---|---|---|
| 目标客户群（多选） | ✗ | ✓ {客户特点} | 仅手写话术时引用 |
| 常见问题 FAQ | ✗ | ✓ {常见问题} | 仅手写话术时引用 |
| 目标客户·所属行业 | ✗ | ✓ {客户行业} | 仅手写话术时引用 |
| 目标客户·所在区域 | ✗ | ✓ {客户区域} | 仅手写话术时引用 |
| 排除关键词 | ✗ | ✓ {排除词} | **采集时过滤线索**（界面完全没说明） |
| 意向关键词 | ✗ | ✓ {意向词} | **采集时给评论打分**（界面完全没说明） |
| 微信号 | ✗（故意，防明文） | ✓ {微信号} | 话术变量用 |
| 主营产品 | ✗ | ✓（兜底） | 基本白填 |
| 业务画像·价格区间 | ✗ | ✓（兜底） | 基本白填 |
| **合规备注** | ✗ | ✗ | **填了纯装饰，谁都不用** |

### P0-3　话术库页头写的「R2 自动切换」是空转功能

**R2 是什么**：它是原始需求文档里给规则编的号（R1/R2/R3 规则引擎）。在话术库这里的含义是一条**自动停用规则**：

> 一条话术变体发出去，如果**加微转化率低于 8%**（阈值可配），且**发送样本 ≥ 30 次**，
> 系统就自动把它停用，权重让给效果好的变体。

举例：

| 变体 | 发送 | 加微 | 转化率 | 结果 |
|---|---|---|---|---|
| A | 100 次 | 12 次 | 12% | 保留 |
| B | 51 次 | 3 次 | 5.9% | 5.9% < 8% 且 51 ≥ 30 → **自动停用** |

页头现在写着：`变体 AB 自动追踪，R2 命中自动切换（转化率 < 8% 且样本 ≥ 30）`。

核实代码后：变体的 `转化率 / 回复数 / 加微数 / 样本达标` 这四个字段，**全项目没有任何一处写入**——只有「发送数」会 +1。

结果：
- 每条变体右侧的「转化率」永远显示 `—`；
- R2 永远不会触发（因为转化率永远是空的）；
- 你之所以觉得它像能用，是因为 `mock-data.js` 里塞了一条**演示数据**（变体 B 转化率 5.9%、样本 51、带 R2 说明），看着像跑通了。

**要么把统计补上，要么把这句话从界面上撤掉——现在是给你看了一个不存在的功能。**

### P1-1　缺了"写话术最需要"的素材

现在 25 个字段里**没有**这几样，但它们恰恰是评论回复/私信最缺的：

- **自我介绍 / 账号人设**——评论回复第一句得报家门，现在没地方写
- **服务流程与交付周期**——客户最常问"多久能做完"，现在没地方写
- **成功案例 / 客户见证**——说服力素材，现在只能硬塞进 FAQ
- **优惠 / 钩子**——引导加微的理由只有 7 个预设，且是装修行业专用
- **不接什么样的客户**——排除只有关键词，没有业务语义

### P1-2　术语和门槛不对一人公司的路子

- 「业务画像 / 产品知识库 / 目标客户 / 微信转化」是咨询公司术语，不是老板的话
- 25 个字段平铺 4 张卡，**没有必填/选填分级**，不知道该先填哪个
- 记录集（多条 + 主记录）对单一业务用不上，首屏就是"1 条空记录"
- 「变体 AB / 转化率 / 样本 ≥ 30」对一天几条线索的量级没有意义

### P1-3　填完看不到反馈

- 填了画像 → 看不出话术变好了
- 就绪条说「五类场景已就绪 5/5」，但话术是**通用**的 → **假绿灯**
- AI 生成时未填字段直接把「（未填）」写进提示词 → 生成通用话术，你以为是自己配置的结果

---

## 三、优化方案

### 方案 A（推荐）· 去重 + 标用途 + 补素材

**A1 去重 3 处**（填了白填的那些）

| 概念 | 保留 | 删除 |
|---|---|---|
| 产品 | 产品知识库「产品名称」 | 业务画像「主营产品」（业务画像卡标题改用行业） |
| 价格 | 产品知识库「价格区间」 | 业务画像「价格区间」 |
| 目标客户 | 业务画像「目标客户」 | 产品知识库「目标客户群」 |

> 同时把「目标客户」从单选改成**多选标签**，保住原来多选的能力。

**A2 给每个字段加「用途徽章」**，鼠标一放就知道填了有什么用：

- `影响 AI 生成` —— 决定 AI 写出来的话术
- `只作话术变量` —— 手写话术里 `{变量}` 能取到
- `采集过滤用` —— 给评论打分 / 排除线索
- `暂未使用` —— 如实标出来（比如合规备注），别让人白填

**A3 补 5 个 AI 真正需要的素材字段**（都进 AI 提示词）

1. 自我介绍 / 账号人设（一句话）
2. 服务流程与交付周期
3. 成功案例 / 客户见证（一行一条）
4. 优惠 / 钩子（引导加微的理由）
5. 不接什么样的客户

**A4 首屏只要求填 6 项「必填」**，其余折叠为选填

> 行业 · 产品名称 · 价格区间 · 目标客户 · 核心卖点 · 主要痛点
>
> 这 6 项填完，AI 话术就有针对性了。其余字段默认收起，需要时再展开。

**A5 就绪条改口径**：只算「通用兜底话术」不算真就绪

现在只要某类场景有 1 条话术就亮绿灯，哪怕它是通用兜底。改成：
- 该场景有话术 **且** 引用的画像变量都已填 → 才算就绪
- 否则显示「话术是通用的，填完业务画像会更有针对性」+ 一键生成

**A6 页头撤掉 R2 那句话**（或按方案 B 补统计）

---

### 方案 B（最小改动，低风险）

只做三件不动字段集的事：

1. 撤掉页头「R2 命中自动切换…」的描述（或改成"变体按权重随机分配"）
2. 就绪条口径修正（通用话术不算就绪）
3. 把重复字段的**说明文字**补上（不删字段，只标清楚哪个生效）

> 半天可完成，风险最低，但字段重复和幽灵字段还在。

---

### 方案 C（大改）· 改成问答式引导

把 4 张卡改成一条引导流程：

```
你是做什么的？ → 卖给谁？ → 凭什么选你？ → 怎么加到你？
        ↓              ↓            ↓            ↓
      行业/区域      客户/痛点     卖点/案例     微信/钩子
```

每答完一步，右侧**实时生成一版话术预览**，让你当场看到"填了有什么用"。
答完即得到一套完整话术。

> 效果最好，但等于重做这一页，工作量最大。

---

## 四、需要你先定的三件事

1. **去重时以哪张卡为准？**
   我建议：产品/价格 → 收到「产品知识库」；目标客户 → 收到「业务画像」。
   （这样 4 张卡职责变成：我是谁 / 卖什么 / 卖给谁 / 怎么加我）

2. **R2 变体统计：补上，还是撤掉？**
   - 撤掉：删掉页头描述 + 变体行的转化率列（10 分钟）
   - 补上：每次真发评论后回写变体统计（要做统计聚合 + 回写，约 1 天）

3. **走 A / B / C？**

---

## 五、建议

如果只想先止血：**方案 B**（半天，去掉假功能、修假绿灯）。

如果想一次到位又不想大改：**方案 A**（去重 + 标用途 + 补素材 + 首屏只要求 6 项）。

**方案 C** 建议等 A 跑一段时间、确认真实使用习惯后再做。
