/* ═══════════════════════════════════════════════════════════
   获客系统 v1.0 前端 — 设计系统
   基调：合规安全感 · 克制稳健 · 老板一眼看懂
   配色：暖纸底 + 墨绿主色 + 墨色文字（避开 AI 青紫渐变审美）
   ═══════════════════════════════════════════════════════════ */

:root {
  /* 色板 */
  --paper:        #f6f5f1;   /* 暖纸底 */
  --paper-deep:   #efede6;
  --card:         #ffffff;
  --ink:          #1c2b26;   /* 近黑墨绿 */
  --ink-soft:     #4c5c56;
  --ink-faint:    #8a968f;
  --line:         #e2e0d7;
  --line-strong:  #c9c7bb;

  --brand:        #1e6e52;   /* 松绿 · 主色 */
  --brand-deep:   #14523d;
  --brand-tint:   #e4efe9;
  --brand-tint-2: #f0f6f2;

  --ok:           #1e7d4f;
  --ok-tint:      #e3f2e9;
  --warn:         #b3741a;   /* 赭金 · 警示 */
  --warn-tint:    #f9efdc;
  --danger:       #c03328;   /* 砖红 · 危险 */
  --danger-tint:  #fae8e5;
  --info:         #35597e;
  --info-tint:    #e7eef6;

  /* 字体 */
  --font-body: "PingFang SC", "Microsoft YaHei", "Noto Sans SC", "Source Han Sans SC", sans-serif;
  --font-display: "Songti SC", "Noto Serif SC", "SimSun", serif;

  /* 节奏 */
  --r-xs: 4px;  --r-sm: 8px;  --r-md: 14px;  --r-lg: 22px;  --r-xl: 34px;
  --radius: 10px;
  --radius-lg: 14px;

  --shadow-1: 0 1px 2px rgba(28, 43, 38, .05), 0 1px 6px rgba(28, 43, 38, .04);
  --shadow-2: 0 6px 24px rgba(28, 43, 38, .12);
  --ease: cubic-bezier(.22, 1, .36, 1);

  --sidenav-w: 224px;
  --topbar-h: 60px;
}

* { box-sizing: border-box; margin: 0; padding: 0; }

html { -webkit-text-size-adjust: 100%; }

body {
  font-family: var(--font-body);
  background: var(--paper);
  color: var(--ink);
  font-size: 14px;
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
}

/* ════════ 通用栅格与工具类 ════════ */
.row-stack > * + * { margin-top: 14px; }
.row-stack-sm > * + * { margin-top: 8px; }
.col-grid { display: grid; gap: 14px; }
.muted { color: var(--ink-faint); }
.soft  { color: var(--ink-soft); }
.right { text-align: right; }
.mt-lg { margin-top: 22px; }

/* ════════ 图标系统（内联 SVG 描边风 · 统一 18px） ════════ */
.ico {
  display: inline-flex; align-items: center; justify-content: center;
  width: 18px; height: 18px;
  flex: none;
  color: var(--ink-soft);
}
.ico svg { width: 100%; height: 100%; display: block; }
.ico--sm { width: 14px; height: 14px; }
.ico--lg { width: 22px; height: 22px; }
.ico--brand { color: var(--brand); }
.ico--ink { color: var(--ink); }
.ico--danger { color: var(--danger); }
.ico--warn { color: var(--warn); }
.ico--ok { color: var(--ok); }
.ico--muted { color: var(--ink-faint); }

/* ════════ 页头（统一标题 + 图标锚点 + 副标题 + 操作区） ════════ */
.page-head {
  display: flex; align-items: center; gap: 14px;
  flex-wrap: wrap; margin-bottom: 22px;
  padding-bottom: 14px; border-bottom: 1px solid var(--line);
}
.page-head__ico {
  width: 36px; height: 36px;
  display: inline-flex; align-items: center; justify-content: center;
  border-radius: 10px;
  background: var(--brand-tint);
  color: var(--brand-deep);
  flex: none;
}
.page-head__ico .ico { width: 20px; height: 20px; color: var(--brand-deep); }
.page-head__title { display: flex; flex-direction: column; gap: 2px; min-width: 0; }
.page-head h1 {
  font-family: var(--font-display);
  font-size: 22px; font-weight: 700; letter-spacing: .01em;
  line-height: 1.2;
}
.page-head__desc { color: var(--ink-soft); font-size: 12.5px; }
.page-head__spacer { flex: 1; }
.page-head__actions { display: flex; gap: 8px; align-items: center; flex-wrap: wrap; }

/* ════════ 视图通用容器 ════════ */
.view { display: flex; flex-direction: column; gap: 22px; animation: view-in .3s var(--ease); }
.view > .page-head { margin-bottom: 0; } /* .view 已接管间距 */

/* ════════ 区块标题（卡片内） ════════ */
.section-title {
  display: flex; align-items: center; gap: 8px;
  font-size: 13px; font-weight: 700;
  color: var(--ink-soft);
  text-transform: uppercase; letter-spacing: .08em;
  margin-bottom: 10px;
}
.section-title .ico { width: 14px; height: 14px; }

/* 数字统一等宽对齐，账目感 */
.num, td.num, .metric-value { font-variant-numeric: tabular-nums; letter-spacing: .01em; }

a { color: var(--brand); text-decoration: none; }
button { font-family: inherit; font-size: inherit; cursor: pointer; }
:focus-visible { outline: 2px solid var(--brand); outline-offset: 2px; border-radius: 4px; }

/* ══════════ 布局骨架 ══════════ */
.shell {
  display: grid;
  grid-template-columns: var(--sidenav-w) 1fr;
  grid-template-rows: var(--topbar-h) 1fr;
  grid-template-areas:
    "topbar topbar"
    "sidenav main";
  min-height: 100vh;
}

/* ══════════ 安全模式横幅（IMP-004） ══════════ */
.safe-banner {
  position: relative;
  z-index: 60;
  display: flex;
  align-items: center;
  gap: 14px;
  padding: 10px 22px;
  background: linear-gradient(0deg, #a52a20, #a52a20);
  color: #fff8f6;
  font-size: 13px;
}
.safe-banner[hidden] { display: none; }
.safe-banner__dot {
  width: 9px; height: 9px; border-radius: 50%;
  background: #ffd9d3; flex: none;
  animation: pulse 1.6s var(--ease) infinite;
}
.safe-banner__text { flex: 1; display: flex; gap: 10px; flex-wrap: wrap; align-items: baseline; }
.safe-banner__text strong { font-size: 13.5px; }
.safe-banner__text span { opacity: .85; }
@keyframes pulse { 0%,100% { transform: scale(1); opacity: 1; } 50% { transform: scale(1.5); opacity: .5; } }

/* ══════════ 顶部栏 ══════════ */
.topbar {
  grid-area: topbar;
  display: flex;
  align-items: center;
  gap: 18px;
  padding: 0 22px;
  background: var(--card);
  border-bottom: 1px solid var(--line);
  position: sticky; top: 0; z-index: 50;
}
.topbar__brand { display: flex; align-items: center; gap: 10px; min-width: 0; }
.brand-text { display: flex; flex-direction: column; line-height: 1.25; }
.brand-text__name { font-family: var(--font-display); font-weight: 700; font-size: 17px; letter-spacing: .02em; }
.brand-text__sub { font-size: 11px; color: var(--ink-faint); }

.topbar__status { display: flex; gap: 8px; margin-left: auto; flex-wrap: wrap; }
.status-pill {
  display: inline-flex; align-items: center; gap: 6px;
  padding: 3px 11px; border-radius: 99px;
  font-size: 12px; border: 1px solid var(--line);
  background: var(--paper); color: var(--ink-soft);
  white-space: nowrap;
}
.status-pill__led { width: 7px; height: 7px; border-radius: 50%; background: var(--ok); }
.status-pill--running { border-color: #bcd9c8; background: var(--ok-tint); color: #14532f; }
.status-paused .status-pill__led { background: var(--warn); }
.status-paused .status-pill--running { border-color: #ecd9b4; background: var(--warn-tint); color: #7a4d0c; }
.status-pill--danger { border-color: #ecc7c2; background: var(--danger-tint); color: var(--danger); }
.status-pill--muted { color: var(--ink-faint); }

.topbar__actions { display: flex; gap: 10px; align-items: center; }

/* ══════════ 按钮 ══════════ */
.btn {
  display: inline-flex; align-items: center; gap: 7px;
  padding: 7px 16px;
  border-radius: 8px;
  border: 1px solid var(--line-strong);
  background: var(--card);
  color: var(--ink);
  font-size: 13.5px;
  transition: background .15s var(--ease), border-color .15s var(--ease), transform .1s var(--ease);
}
.btn:hover { background: var(--paper-deep); }
.btn:active { transform: translateY(1px); }
.btn:disabled { opacity: .45; cursor: not-allowed; transform: none; }

.btn--primary { background: var(--brand); border-color: var(--brand-deep); color: #fff; font-weight: 600; }
.btn--primary:hover { background: var(--brand-deep); }

.btn--danger-outline { border-color: #d8a49d; color: var(--danger); background: var(--danger-tint); font-weight: 600; }
.btn--danger-outline:hover { background: #f4d9d4; }

.btn--ghost-light { border-color: rgba(255,255,255,.55); background: transparent; color: #fff; }
.btn--ghost-light:hover { background: rgba(255,255,255,.14); }

.btn--sm { padding: 5px 12px; font-size: 12.5px; }
.btn--lg { padding: 11px 26px; font-size: 15px; border-radius: 10px; }

.btn-link { border: none; background: none; color: var(--brand); padding: 2px 4px; font-size: 13px; }
.btn-link:hover { text-decoration: underline; }

/* ══════════ 左导航 ══════════ */
.sidenav {
  grid-area: sidenav;
  background: var(--card);
  border-right: 1px solid var(--line);
  display: flex; flex-direction: column;
  padding: var(--r-lg) 12px 14px;
  position: sticky; top: var(--topbar-h);
  height: calc(100vh - var(--topbar-h));
}
.sidenav__list { list-style: none; display: flex; flex-direction: column; gap: 2px; }
.sidenav__list a {
  display: flex; align-items: center; gap: 11px;
  padding: 9px 13px;
  border-radius: 8px;
  color: var(--ink-soft);
  font-size: 14px;
  transition: background .15s var(--ease), color .15s var(--ease);
}
.sidenav__list a:hover { background: var(--paper); color: var(--ink); }
.sidenav__list a[aria-current="page"] {
  background: var(--brand-tint);
  color: var(--brand-deep);
  font-weight: 600;
}
.nav-ico { font-size: 15px; width: 20px; text-align: center; display: inline-flex; align-items: center; justify-content: center; }
.nav-ico svg { width: 17px; height: 17px; }
.nav-badge {
  margin-left: auto;
  font-size: 11px; padding: 0 7px; border-radius: 99px;
  background: var(--danger); color: #fff; line-height: 18px;
}
.nav-badge--count { background: var(--line-strong); color: var(--ink-soft); font-weight: 600; }

.sidenav__footer { margin-top: auto; display: flex; flex-direction: column; gap: 10px; }
.north-star {
  border: 1px solid var(--line);
  border-left: 3px solid var(--brand);
  border-radius: var(--radius);
  padding: 10px 13px;
  background: var(--brand-tint-2);
  display: flex; flex-direction: column; gap: 1px;
}
.north-star__label { font-size: 11px; color: var(--ink-faint); letter-spacing: .06em; }
.north-star__value {
  font-family: var(--font-display);
  font-size: 30px; font-weight: 700; color: var(--brand-deep);
  line-height: 1.15;
}
.north-star__delta { font-size: 11.5px; color: var(--ok); }
.sidenav__version { font-size: 11px; color: var(--ink-faint); line-height: 1.5; }

/* ══════════ 主内容区 ══════════ */
.main {
  grid-area: main;
  padding: var(--r-lg) clamp(16px, 3vw, 34px) 60px;
  min-width: 0;
  outline: none;
}

/* 子页签（看板三视图） */
.subtabs { display: flex; gap: 2px; border-bottom: 1px solid var(--line); margin-bottom: var(--r-md); }
.subtabs button {
  border: none; background: none;
  padding: 8px 16px 10px;
  color: var(--ink-faint); font-size: 14px;
  border-bottom: 2px solid transparent;
  margin-bottom: -1px;
  transition: color .15s var(--ease);
}
.subtabs button:hover { color: var(--ink); }
.subtabs button[aria-selected="true"] { color: var(--brand-deep); font-weight: 600; border-bottom-color: var(--brand); }

/* ══════════ 卡片与网格 ══════════ */
.card {
  background: var(--card);
  border: 1px solid var(--line);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-1);
  padding: var(--r-md) var(--r-lg);
}
.card__head { display: flex; align-items: baseline; gap: 10px; margin-bottom: 12px; }
.card__title { font-size: 15px; font-weight: 700; }
.card__hint { font-size: 12px; color: var(--ink-faint); }
.card__spacer { flex: 1; }

.grid { display: grid; gap: var(--r-md); }
.grid--metrics { grid-template-columns: repeat(auto-fit, minmax(168px, 1fr)); }
.grid--2 { grid-template-columns: repeat(auto-fit, minmax(340px, 1fr)); }
.grid--main-side { grid-template-columns: minmax(0, 1.7fr) minmax(280px, 1fr); }
@media (max-width: 980px) { .grid--main-side { grid-template-columns: 1fr; } }

/* 指标卡 */
.metric { display: flex; flex-direction: column; gap: 2px; }
.metric__label { font-size: 12px; color: var(--ink-faint); }
.metric-value {
  font-family: var(--font-display);
  font-size: 30px; font-weight: 700; line-height: 1.2;
}
.metric-value--sm { font-size: 24px; }
.metric__foot { font-size: 11.5px; color: var(--ink-faint); }
.metric__delta--up { color: var(--ok); }
.metric__delta--down { color: var(--danger); }

/* ══════════ 标签 / 状态 ══════════ */
.tag {
  display: inline-flex; align-items: center; gap: 5px;
  font-size: 11.5px; font-weight: 600;
  padding: 1px 9px; border-radius: 99px;
  white-space: nowrap; line-height: 20px;
}
.tag--collected  { background: var(--paper-deep);   color: var(--ink-soft); }
.tag--pending    { background: var(--info-tint);    color: var(--info); }
.tag--throttled  { background: var(--warn-tint);    color: var(--warn); }
.tag--sent       { background: var(--info-tint);    color: var(--info); }
.tag--replied    { background: var(--brand-tint);   color: var(--brand-deep); }
.tag--wechat     { background: var(--ok-tint);      color: var(--ok); }
.tag--deal       { background: var(--brand);        color: #fff; }
.tag--failed     { background: var(--danger-tint);  color: var(--danger); }
.tag--rejected   { background: var(--paper-deep);   color: var(--ink-faint); }
.tag--healthy    { background: var(--ok-tint);      color: var(--ok); }
.tag--paused     { background: var(--warn-tint);    color: var(--warn); }
.tag--safemode   { background: var(--danger-tint);  color: var(--danger); }
.tag--banned     { background: #3d3a37;             color: #ffd9d3; }
.tag--high       { background: var(--danger-tint);  color: var(--danger); }
.tag--mid        { background: var(--warn-tint);    color: var(--warn); }
.tag--low        { background: var(--paper-deep);   color: var(--ink-faint); }

/* ══════════ 表格 ══════════ */
.table-wrap { overflow-x: auto; }
table.data { width: 100%; border-collapse: collapse; font-size: 13px; }
table.data th {
  text-align: left; font-size: 12px; color: var(--ink-faint);
  font-weight: 600; padding: 7px 10px;
  border-bottom: 1px solid var(--line-strong);
  white-space: nowrap;
}
table.data td { padding: 9px 10px; border-bottom: 1px solid var(--line); vertical-align: middle; }
table.data tr:last-child td { border-bottom: none; }
table.data tbody tr { transition: background .12s var(--ease); }
table.data tbody tr:hover { background: var(--brand-tint-2); }
table.data tbody tr.row--danger { background: #fdf6f5; }
table.data tbody tr.row--danger:hover { background: #faeeec; }

.cell-user { display: flex; align-items: center; gap: 9px; }
.avatar {
  width: 28px; height: 28px; border-radius: 50%;
  display: inline-flex; align-items: center; justify-content: center;
  font-size: 12px; font-weight: 700; color: #fff; flex: none;
}
.cell-comment { max-width: 320px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; color: var(--ink-soft); }

/* ══════════ 漏斗图 ══════════ */
.funnel { display: flex; flex-direction: column; gap: 8px; }
.funnel__row { display: grid; grid-template-columns: 108px 1fr 148px; align-items: center; gap: 12px; }
.funnel__label { font-size: 13px; color: var(--ink-soft); text-align: right; }
.funnel__bar-track { height: 30px; background: var(--paper-deep); border-radius: 6px; overflow: hidden; }
.funnel__bar {
  height: 100%; border-radius: 6px;
  background: var(--brand);
  display: flex; align-items: center;
  padding-left: 10px;
  color: #fff; font-weight: 700; font-size: 13px;
  min-width: 42px;
  transform-origin: left;
  animation: funnel-grow .7s var(--ease) both;
}
.funnel__row:nth-child(2) .funnel__bar { background: #2b7f60; animation-delay: .06s; }
.funnel__row:nth-child(3) .funnel__bar { background: #3f9070; animation-delay: .12s; }
.funnel__row:nth-child(4) .funnel__bar { background: #58a186; animation-delay: .18s; }
.funnel__row:nth-child(5) .funnel__bar { background: #74b39c; animation-delay: .24s; }
.funnel__row:nth-child(6) .funnel__bar { background: var(--danger); animation-delay: .30s; }
@keyframes funnel-grow { from { transform: scaleX(0); } to { transform: scaleX(1); } }
.funnel__conv { font-size: 12px; color: var(--ink-faint); }
.funnel__conv b { color: var(--brand-deep); font-size: 13px; }

/* ══════════ 折线图（SVG） ══════════ */
.chart-svg { width: 100%; height: auto; display: block; }
.chart-line { fill: none; stroke: var(--brand); stroke-width: 2.2; stroke-linecap: round; stroke-linejoin: round; }
.chart-area { fill: url(#areaGrad); opacity: .9; }
.chart-grid { stroke: var(--line); stroke-width: 1; }
.chart-axis { font-size: 10px; fill: var(--ink-faint); font-family: var(--font-body); }
.chart-dot { fill: var(--card); stroke: var(--brand); stroke-width: 2; }

/* ══════════ 健康度仪表 ══════════ */
.health-list { display: flex; flex-direction: column; }
.health-row {
  display: grid; grid-template-columns: minmax(120px, 1.2fr) 130px 1fr auto;
  gap: 14px; align-items: center;
  padding: 11px 4px; border-bottom: 1px solid var(--line);
}
.health-row:last-child { border-bottom: none; }
.gauge { display: flex; align-items: center; gap: 8px; }
.gauge__num { font-family: var(--font-display); font-size: 19px; font-weight: 700; width: 34px; text-align: right; }
.gauge__num--ok { color: var(--ok); }
.gauge__num--warn { color: var(--warn); }
.gauge__num--danger { color: var(--danger); }
.gauge__track { flex: 1; height: 7px; border-radius: 99px; background: var(--paper-deep); overflow: hidden; min-width: 60px; }
.gauge__fill { height: 100%; border-radius: 99px; transition: width .8s var(--ease); }

/* 健康度构成小格 */
.factor-grid { display: flex; gap: 5px; flex-wrap: wrap; }
.factor {
  font-size: 10.5px; padding: 1px 7px; border-radius: 5px;
  background: var(--paper-deep); color: var(--ink-soft);
}
.factor--good { background: var(--ok-tint); color: var(--ok); }
.factor--warn  { background: var(--warn-tint); color: var(--warn); }
.factor--bad   { background: var(--danger-tint); color: var(--danger); }

/* ══════════ 审计 / 事件流 ══════════ */
.timeline { list-style: none; display: flex; flex-direction: column; }
.timeline li {
  display: grid; grid-template-columns: 14px 1fr auto; gap: 10px;
  padding: 8px 0; border-bottom: 1px dashed var(--line);
  font-size: 12.5px; align-items: baseline;
}
.timeline li:last-child { border-bottom: none; }
.timeline__dot { width: 8px; height: 8px; border-radius: 50%; margin-top: 5px; justify-self: center; }
.timeline__time { color: var(--ink-faint); font-size: 11.5px; white-space: nowrap; }

/* ══════════ 表单 ══════════ */
.field { display: flex; flex-direction: column; gap: 5px; margin-bottom: 14px; }
.field > label { font-size: 12.5px; font-weight: 600; color: var(--ink-soft); }
.field__hint { font-size: 11.5px; color: var(--ink-faint); }
input[type="text"], input[type="number"], input[type="url"], select, textarea {
  font-family: inherit; font-size: 13.5px;
  padding: 8px 12px;
  border: 1px solid var(--line-strong);
  border-radius: 8px;
  background: var(--card);
  color: var(--ink);
  transition: border-color .15s var(--ease), box-shadow .15s var(--ease);
  width: 100%;
}
input:focus, select:focus, textarea:focus {
  outline: none; border-color: var(--brand);
  box-shadow: 0 0 0 3px var(--brand-tint);
}
textarea { resize: vertical; min-height: 74px; line-height: 1.6; }
.radio-cards { display: grid; gap: 8px; }
.radio-card {
  display: flex; gap: 10px; align-items: flex-start;
  border: 1.5px solid var(--line); border-radius: 10px;
  padding: 11px 13px; cursor: pointer;
  transition: border-color .15s var(--ease), background .15s var(--ease);
}
.radio-card:hover { border-color: var(--line-strong); }
.radio-card input { margin-top: 4px; accent-color: var(--brand); }
.radio-card--checked { border-color: var(--brand); background: var(--brand-tint-2); }
.radio-card__body { display: flex; flex-direction: column; gap: 3px; }
.radio-card__title { font-weight: 600; font-size: 13.5px; }
.radio-card__desc { font-size: 12px; color: var(--ink-soft); }

/* ══════════ 向导（IMP-011） ══════════ */
.wizard { max-width: 720px; margin: 0 auto; }
.wizard__steps { display: flex; gap: 0; margin-bottom: var(--r-lg); }
.wizard__step {
  flex: 1; display: flex; flex-direction: column; align-items: center; gap: 6px;
  position: relative; font-size: 12px; color: var(--ink-faint);
  padding-top: 2px;
}
.wizard__step::before {
  content: ""; position: absolute; top: 14px; left: -50%; width: 100%; height: 2px;
  background: var(--line);
}
.wizard__step:first-child::before { display: none; }
.wizard__step--done::before, .wizard__step--active::before { background: var(--brand); }
.wizard__step-no {
  width: 28px; height: 28px; border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  background: var(--card); border: 2px solid var(--line-strong);
  font-weight: 700; font-size: 12.5px; color: var(--ink-faint);
  position: relative; z-index: 1;
}
.wizard__step--active .wizard__step-no { border-color: var(--brand); background: var(--brand); color: #fff; }
.wizard__step--done .wizard__step-no { border-color: var(--brand); background: var(--brand-tint); color: var(--brand-deep); }
.wizard__step--active { color: var(--brand-deep); font-weight: 600; }
.wizard__step--done { color: var(--brand); }
.wizard__panel { animation: view-in .35s var(--ease); }
.wizard__foot { display: flex; justify-content: space-between; margin-top: var(--r-lg); }

@keyframes view-in { from { opacity: 0; transform: translateY(8px); } to { opacity: 1; transform: none; } }

/* ══════════ 抽屉 ══════════ */
.drawer-backdrop, .modal-backdrop {
  position: fixed; inset: 0; z-index: 90;
  background: rgba(28, 43, 38, .38);
  opacity: 0; transition: opacity .25s var(--ease);
}
.drawer-backdrop[hidden], .modal-backdrop[hidden] { display: none; }
.drawer-backdrop.show, .modal-backdrop.show { opacity: 1; }

.drawer {
  position: fixed; top: 0; right: 0; bottom: 0; z-index: 95;
  width: min(480px, 94vw);
  background: var(--card);
  border-left: 1px solid var(--line);
  box-shadow: var(--shadow-2);
  transform: translateX(100%);
  transition: transform .3s var(--ease);
  display: flex; flex-direction: column;
  overflow-y: auto;
}
.drawer[hidden] { display: none; }
.drawer.open { transform: translateX(0); }
.drawer__head {
  position: sticky; top: 0; background: var(--card);
  display: flex; align-items: center; gap: 12px;
  padding: 16px 22px; border-bottom: 1px solid var(--line);
  z-index: 2;
}
.drawer__title { font-size: 16px; font-weight: 700; flex: 1; }
.drawer__close { border: none; background: none; font-size: 20px; color: var(--ink-faint); padding: 4px 8px; border-radius: 6px; }
.drawer__close:hover { background: var(--paper); color: var(--ink); }
.drawer__body { padding: 18px 22px 40px; display: flex; flex-direction: column; gap: 16px; }

.msg-list { display: flex; flex-direction: column; gap: 10px; }
.msg {
  max-width: 82%; padding: 8px 13px; border-radius: 12px;
  font-size: 13px; line-height: 1.55;
}
.msg--out { align-self: flex-end; background: var(--brand-tint); color: var(--brand-deep); border-bottom-right-radius: 3px; }
.msg--in  { align-self: flex-start; background: var(--paper-deep); border-bottom-left-radius: 3px; }
.msg__meta { font-size: 10.5px; color: var(--ink-faint); margin-top: 3px; }

.kv { display: grid; grid-template-columns: 88px 1fr; gap: 6px 12px; font-size: 13px; }
.kv dt { color: var(--ink-faint); }
.kv dd { color: var(--ink); }

/* ══════════ 弹窗 ══════════ */
.modal {
  position: fixed; z-index: 96;
  top: 50%; left: 50%;
  transform: translate(-50%, -46%) scale(.97);
  width: min(560px, 92vw);
  max-height: 86vh; overflow-y: auto;
  background: var(--card);
  border-radius: var(--radius-lg);
  border: 1px solid var(--line);
  box-shadow: var(--shadow-2);
  padding: var(--r-lg);
  opacity: 0; pointer-events: none;
  transition: opacity .22s var(--ease), transform .22s var(--ease);
}
.modal[hidden] { display: none; }
.modal.show { opacity: 1; pointer-events: auto; transform: translate(-50%, -50%) scale(1); }
.modal h2 { font-family: var(--font-display); font-size: 18px; margin-bottom: 10px; }
.modal__foot { display: flex; justify-content: flex-end; gap: 10px; margin-top: 20px; }

/* 确认退出安全模式 */
.confirm-note {
  background: var(--warn-tint); border: 1px solid #ecd9b4;
  border-radius: 8px; padding: 10px 14px; font-size: 12.5px; color: #7a4d0c;
}

/* ══════════ 话术卡片 ══════════ */
.script-card { display: flex; flex-direction: column; gap: 10px; }
.script-text {
  background: var(--brand-tint-2); border: 1px dashed var(--line-strong);
  border-radius: 10px; padding: 11px 14px;
  font-size: 13.5px; line-height: 1.7; color: var(--ink);
}
.script-meta { display: flex; gap: 14px; flex-wrap: wrap; font-size: 12px; color: var(--ink-faint); }
.script-meta b { color: var(--ink); font-variant-numeric: tabular-nums; }
.variant-row {
  display: grid; grid-template-columns: auto 1fr auto; gap: 12px; align-items: center;
  padding: 10px 2px; border-top: 1px solid var(--line);
}
.variant-row__body { min-width: 0; }
.r2-flag {
  display: inline-flex; align-items: center; gap: 5px;
  font-size: 11.5px; font-weight: 700; color: var(--warn);
  background: var(--warn-tint); border-radius: 6px; padding: 2px 9px;
}

/* ══════════ 空状态（统一大圆图章式） ══════════ */
.empty {
  text-align: center; padding: 44px 20px; color: var(--ink-faint);
  display: flex; flex-direction: column; align-items: center; gap: 8px;
}
.empty__ico {
  width: 56px; height: 56px;
  display: inline-flex; align-items: center; justify-content: center;
  border-radius: 50%;
  background: var(--paper-deep);
  color: var(--ink-faint);
}
.empty__ico .ico { width: 28px; height: 28px; color: var(--ink-faint); }
.empty p { font-size: 13px; }

/* ══════════ 轻提示 ══════════ */
.toast-region { position: fixed; bottom: 24px; left: 50%; transform: translateX(-50%); z-index: 120; display: flex; flex-direction: column; gap: 8px; align-items: center; }
.toast {
  background: var(--ink); color: var(--paper);
  border-radius: 99px; padding: 8px 22px;
  font-size: 13px; box-shadow: var(--shadow-2);
  animation: toast-in .3s var(--ease);
}
.toast--ok::before { content: "✓ "; color: #7dd8a8; }
.toast--warn::before { content: "⚠ "; color: #f0c069; }
@keyframes toast-in { from { opacity: 0; transform: translateY(12px); } to { opacity: 1; transform: none; } }

/* ══════════ 筛选条 ══════════ */
.filter-bar { display: flex; gap: 8px; flex-wrap: wrap; align-items: center; margin-bottom: var(--r-md); }
.filter-chip {
  border: 1px solid var(--line-strong); background: var(--card);
  border-radius: 99px; padding: 4px 14px; font-size: 12.5px; color: var(--ink-soft);
  transition: all .15s var(--ease);
}
.filter-chip:hover { border-color: var(--brand); color: var(--brand-deep); }
.filter-chip[aria-pressed="true"] { background: var(--brand); border-color: var(--brand-deep); color: #fff; font-weight: 600; }
.filter-chip .count { opacity: .7; margin-left: 3px; }

/* ══════════ 响应式 ══════════ */
@media (max-width: 860px) {
  .shell {
    grid-template-columns: 1fr;
    grid-template-areas: "topbar" "sidenav" "main";
  }
  .sidenav {
    position: static; height: auto;
    flex-direction: row; align-items: center;
    padding: 8px 12px; border-right: none; border-bottom: 1px solid var(--line);
    overflow-x: auto;
  }
  .sidenav__list { flex-direction: row; gap: 4px; }
  .sidenav__list a { padding: 6px 11px; font-size: 13px; }
  .nav-ico { display: none; }
  .nav-group { display: none; }
  .sidenav__footer { display: none; }
  .brand-text__sub { display: none; }
  .topbar { flex-wrap: wrap; height: auto; padding: 10px 14px; }
  .shell { grid-template-rows: auto auto 1fr; }
  .funnel__row { grid-template-columns: 86px 1fr; }
  .funnel__conv { grid-column: 2; }
  .health-row { grid-template-columns: 1fr 110px; }
  .health-row .factor-grid { display: none; }
}

/* ════════ 导航分组（业务域） ════════ */
.nav-group {
  margin: 16px 12px 5px;
  font-size: 11px; font-weight: 700;
  letter-spacing: .14em;
  color: var(--ink-faint);
  user-select: none;
}
.sidenav .nav-group + .sidenav__list { margin-top: 0; }

/* ════════ 商机管线（阶段卡） ════════ */
.pipeline { display: flex; gap: 8px; overflow-x: auto; padding-bottom: 4px; margin-bottom: var(--r-md); }
.stage-card {
  flex: 1 0 106px;
  background: var(--card);
  border: 1px solid var(--line);
  border-radius: 10px;
  padding: 10px 13px;
  box-shadow: var(--shadow-1);
  cursor: pointer; text-align: left;
  display: flex; flex-direction: column; gap: 1px;
  transition: border-color .15s var(--ease), background .15s var(--ease), transform .1s var(--ease);
  font-family: inherit;
}
.stage-card:hover { border-color: var(--line-strong); }
.stage-card:active { transform: translateY(1px); }
.stage-card[aria-pressed="true"] { border-color: var(--brand); background: var(--brand-tint-2); }
.stage-card--won[aria-pressed="true"] { border-color: var(--brand-deep); }
.stage-card--lost { opacity: .78; }
.stage-card__count { font-family: var(--font-display); font-size: 22px; font-weight: 700; line-height: 1.25; }
.stage-card__label { font-size: 12px; color: var(--ink-soft); }
.stage-card__sub { font-size: 11px; color: var(--ink-faint); }

/* ════════ 阶段步进器（客户详情） ════════ */
.stepper { display: flex; gap: 5px; flex-wrap: wrap; }
.step-pill {
  font-size: 12px; padding: 3px 11px; border-radius: 99px;
  border: 1px solid var(--line); color: var(--ink-faint); background: var(--card);
  white-space: nowrap;
}
.step-pill--done { background: var(--brand-tint); color: var(--brand-deep); border-color: #bcd9c8; }
.step-pill--current { background: var(--brand); color: #fff; border-color: var(--brand-deep); font-weight: 700; }
.step-pill--lost { background: var(--paper-deep); color: var(--ink-faint); }

/* ════════ 待办清单（今日跟进 / 工作台） ════════ */
.todo { display: flex; flex-direction: column; }
.todo-item {
  display: grid; grid-template-columns: 15px 1fr auto; gap: 12px;
  align-items: center;
  padding: 11px 4px; border-bottom: 1px dashed var(--line);
  font-size: 13px;
}
.todo-item:last-child { border-bottom: none; }
.todo-item--overdue .todo__text { color: var(--danger); }
.todo-item--done { opacity: .5; }
.todo__check {
  width: 16px; height: 16px; border-radius: 5px;
  border: 1.5px solid var(--line-strong); background: var(--card);
  appearance: none; cursor: pointer; margin: 0;
  display: inline-flex; align-items: center; justify-content: center;
  transition: all .15s var(--ease);
}
.todo__check:checked { background: var(--brand); border-color: var(--brand-deep); }
.todo__check:checked::after { content: "✓"; color: #fff; font-size: 11px; font-weight: 700; }
.todo__text b { display: block; font-weight: 600; }
.todo__meta { font-size: 11.5px; color: var(--ink-faint); }
.todo__due { font-size: 12px; color: var(--ink-faint); white-space: nowrap; }
.todo__due--overdue { color: var(--danger); font-weight: 700; }

/* ════════ 归因条形行（每个视频占 1 行，包含主条 + 副指标） ════════ */
.bar-row {
  display: grid; grid-template-columns: 156px 1fr 90px;
  gap: 12px; align-items: center;
  padding: 10px 0; font-size: 13px;
  border-bottom: 1px dashed var(--line);
}
.bar-row:last-child { border-bottom: none; }
.bar-row__label { color: var(--ink); display: flex; align-items: center; gap: 8px; min-width: 0; }
.bar-row__label > span { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.bar-row__label b { color: var(--ink-faint); font-family: var(--font-display); font-size: 16px; min-width: 18px; }
.bar-row__main { display: flex; flex-direction: column; gap: 4px; min-width: 0; }
.bar-row__title { font-weight: 600; color: var(--ink); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.bar-row__sub { font-size: 11.5px; color: var(--ink-faint); display: flex; gap: 10px; flex-wrap: wrap; }
.bar-track { height: 8px; background: var(--paper-deep); border-radius: 99px; overflow: hidden; }
.bar-fill { height: 100%; background: var(--brand); border-radius: 99px; min-width: 3px; }
.bar-fill--wechat { background: #58a186; }
.bar-row__value { text-align: right; color: var(--brand-deep); font-weight: 700; font-variant-numeric: tabular-nums; }
.bar-row__value small { display: block; color: var(--ink-faint); font-weight: 400; font-size: 11.5px; }

/* ════════ 账号卡（独立布局，不复用 healthRow） ════════ */
.acc-card {
  background: var(--card);
  border: 1px solid var(--line);
  border-radius: var(--radius-lg);
  padding: var(--r-md) var(--r-lg);
  box-shadow: var(--shadow-1);
  display: flex; flex-direction: column; gap: 14px;
  transition: border-color .15s var(--ease);
}
.acc-card--danger { border-color: #e5b7b0; background: #fdfaf9; }
.acc-card__head { display: flex; align-items: center; gap: 12px; }
.acc-card__head .avatar { width: 38px; height: 38px; font-size: 15px; }
.acc-card__name { font-weight: 700; }
.acc-card__sub { font-size: 11.5px; color: var(--ink-faint); }
.acc-card__spacer { flex: 1; }
.acc-card__row {
  display: grid; grid-template-columns: auto 1fr auto; gap: 12px;
  align-items: center;
}
.acc-card__row .gauge__num { font-size: 20px; }
.acc-card__row .gauge__track { display: block; flex: 1; }
.acc-card__meta { font-size: 11.5px; color: var(--ink-faint); white-space: nowrap; }
.acc-card__note { font-size: 12px; color: var(--ink-soft); }
.acc-card__footer {
  display: flex; justify-content: space-between; align-items: center;
  padding-top: 10px; border-top: 1px dashed var(--line);
  gap: 10px; flex-wrap: wrap;
}

/* ════════ 工作台预警项 ════════ */
.alert-item {
  display: flex; gap: 10px; align-items: flex-start;
  padding: 10px 0; border-bottom: 1px dashed var(--line);
  font-size: 13px;
}
.alert-item:last-child { border-bottom: none; }
.alert-item__dot { width: 8px; height: 8px; border-radius: 50%; margin-top: 6px; flex: none; }
.alert-item__text { flex: 1; color: var(--ink); }
.alert-item .btn-link { white-space: nowrap; flex: none; margin-top: 1px; }

/* ════════ 私信对话预览行 ════════ */
.dm-conv {
  display: grid; grid-template-columns: auto 1fr auto; gap: 12px; align-items: center;
  padding: 11px 4px; border-bottom: 1px dashed var(--line);
  cursor: pointer; transition: background .12s var(--ease);
  border-radius: 6px;
}
.dm-conv:hover { background: var(--brand-tint-2); }
.dm-conv:last-child { border-bottom: none; }
.dm-conv__last { font-size: 12.5px; color: var(--ink-soft); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }

/* 减少动效偏好 */
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after { animation-duration: .01ms !important; transition-duration: .01ms !important; }
}

/* 打印：只留看板主内容 */
@media print {
  .topbar, .sidenav, .toast-region, .subtabs { display: none; }
  .shell { display: block; }
  .main { padding: 0; }
}

/* ════════ 采集配置页 ════════ */
.status-pill--ok { border-color: #bcd9c8; background: var(--ok-tint); color: #14532f; }

.btn--block { display: block; width: 100%; }
.btn--ghost { background: transparent; border-color: var(--line-strong); color: var(--ink-soft); }
.btn--ghost:hover { background: var(--paper-deep); border-color: var(--ink-faint); }

.task-item {
  padding: 14px 16px;
  border-bottom: 1px dashed var(--line);
  display: flex; flex-direction: column; gap: 8px;
}
.task-item:last-child { border-bottom: none; }
.task-item__head { display: flex; align-items: center; justify-content: space-between; gap: 10px; }
.task-item__title { font-weight: 600; font-size: 14px; color: var(--ink); }
.task-item__meta { font-size: 12px; color: var(--ink-faint); }
.task-item__meta b { color: var(--ink-soft); font-weight: 600; }
.task-item__error { font-size: 12px; color: var(--danger); background: var(--danger-tint); padding: 6px 10px; border-radius: 6px; }
.task-item__actions { display: flex; gap: 8px; margin-top: 4px; }

.progress-bar {
  width: 100%; height: 6px; background: var(--paper-deep);
  border-radius: 3px; overflow: hidden;
}
.progress-bar__fill {
  height: 100%; background: linear-gradient(90deg, var(--brand), #3a9d7a);
  border-radius: 3px; transition: width .4s var(--ease);
}

/* ════════ 数据备份与恢复页 ════════ */
.backup-page { max-width: 960px; }

.backup-actions__row {
  display: flex; align-items: center; gap: 12px; flex-wrap: wrap;
}
.backup-action-status {
  font-size: 13px; margin-left: 8px;
}

.backup-settings-row {
  display: flex; align-items: center; gap: 20px; flex-wrap: wrap;
}
.backup-toggle {
  display: flex; align-items: center; gap: 8px; cursor: pointer;
  font-size: 14px;
}
.backup-toggle input[type="checkbox"] {
  width: 18px; height: 18px; accent-color: var(--brand-deep);
}
.backup-settings-field {
  display: flex; align-items: center; gap: 8px; font-size: 13px;
  color: var(--ink-soft);
}
.backup-settings-field input {
  border: 1px solid var(--line); border-radius: 6px;
  padding: 6px 8px; font-size: 13px;
}

.backup-table {
  width: 100%; border-collapse: collapse; font-size: 13px;
}
.backup-table th {
  text-align: left; padding: 10px 8px;
  border-bottom: 2px solid var(--line-strong);
  color: var(--ink-soft); font-weight: 600; font-size: 12px;
  text-transform: uppercase; letter-spacing: .03em;
}
.backup-table td {
  padding: 10px 8px; border-bottom: 1px dashed var(--line);
}
.backup-table tr:hover td { background: var(--brand-tint-2); }
.backup-filename {
  font-family: "SF Mono", "Cascadia Code", Consolas, monospace;
  font-size: 12px; color: var(--ink-soft);
}
.backup-ops { display: flex; gap: 6px; }

.badge {
  display: inline-block; padding: 2px 8px; border-radius: 10px;
  font-size: 11px; font-weight: 600;
}
.badge--info { background: var(--info-tint); color: var(--info); }
.badge--warn { background: var(--warn-tint); color: var(--warn); }
.badge--default { background: var(--paper-deep); color: var(--ink-soft); }

.btn--warn {
  background: var(--warn-tint); border-color: var(--warn);
  color: var(--warn);
}
.btn--warn:hover { background: var(--warn); color: #fff; }

.btn--danger {
  background: var(--danger-tint); border-color: var(--danger);
  color: var(--danger);
}
.btn--danger:hover { background: var(--danger); color: #fff; }

/* ═══════════════════════════════════════════════════════════
   系统监控页面样式（P4-C）· 只追加
   ═══════════════════════════════════════════════════════════ */
.mon-updated { font-size: 12px; color: var(--ink-faint); font-variant-numeric: tabular-nums; }

/* 状态卡片行（5 个小卡片） */
.mon-comp-grid { display: grid; grid-template-columns: repeat(5, 1fr); gap: 12px; margin-bottom: 14px; }
@media (max-width: 1100px) { .mon-comp-grid { grid-template-columns: repeat(2, 1fr); } }
.mon-comp { background: #fff; border: 1px solid var(--line); border-radius: 12px; padding: 13px 14px; }
.mon-comp__head { display: flex; align-items: center; gap: 7px; margin-bottom: 8px; }
.mon-comp__label { font-size: 12.5px; color: var(--ink-soft); }
.mon-comp__status { font-size: 16px; font-weight: 700; font-family: var(--font-display); text-transform: uppercase; letter-spacing: .03em; }
.mon-comp__sub { font-size: 11.5px; color: var(--ink-faint); margin-top: 4px; }
.mon-dot { display: inline-block; width: 9px; height: 9px; border-radius: 50%; flex: 0 0 auto; box-shadow: 0 0 0 3px rgba(0,0,0,.03); }

/* KV 信息 */
.mon-kv-grid { display: grid; grid-template-columns: 1fr; gap: 8px; margin-bottom: 12px; }
.mon-kv { display: flex; justify-content: space-between; gap: 12px; font-size: 12.5px; }
.mon-kv__k { color: var(--ink-faint); flex: 0 0 auto; }
.mon-kv__v { color: var(--ink-soft); text-align: right; word-break: break-all; }
.mon-path { font-family: ui-monospace, Consolas, monospace; font-size: 11px; }
.mon-table-list { display: flex; flex-direction: column; gap: 4px; max-height: 220px; overflow-y: auto; }
.mon-table-row { display: flex; justify-content: space-between; font-size: 12px; padding: 3px 0; border-bottom: 1px dashed var(--line); }
.mon-table-name { color: var(--ink-soft); font-family: ui-monospace, Consolas, monospace; }

/* API 统计 */
.mon-kpi-row { display: flex; gap: 18px; margin-bottom: 12px; }
.mon-kpi { display: flex; flex-direction: column; }
.mon-kpi__v { font-size: 22px; font-weight: 700; font-family: var(--font-display); color: var(--brand-deep); }
.mon-kpi__k { font-size: 11.5px; color: var(--ink-faint); }
.mon-ap-list { display: flex; flex-direction: column; gap: 5px; }
.mon-ap-row { display: flex; justify-content: space-between; gap: 10px; font-size: 12px; padding: 4px 0; border-bottom: 1px dashed var(--line); }
.mon-ap-endpoint { font-family: ui-monospace, Consolas, monospace; color: var(--ink-soft); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.mon-ap-rate { color: var(--ink-faint); }

/* 任务运行态 */
.mon-task-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px; }
.mon-task-block { border: 1px solid var(--line); border-radius: 10px; padding: 11px 13px; }
.mon-task__t { font-size: 12.5px; font-weight: 600; margin-bottom: 8px; }
.mon-task__nums { display: flex; flex-wrap: wrap; gap: 12px; font-size: 12px; color: var(--ink-soft); }
.mon-task__nums b { color: var(--ink); font-variant-numeric: tabular-nums; }

/* 账号健康度 */
.mon-acc-head, .mon-acc-row { display: grid; grid-template-columns: 2fr 1.6fr 1fr 1fr 1fr; gap: 10px; align-items: center; }
.mon-acc-head { font-size: 11.5px; color: var(--ink-faint); padding: 6px 10px; border-bottom: 1px solid var(--line); }
.mon-acc-row { padding: 9px 10px; border-bottom: 1px dashed var(--line); font-size: 12.5px; }
.mon-acc-row--bad { background: rgba(239, 68, 68, .06); }
.mon-acc-name { font-weight: 600; }
.mon-acc-gauge { display: flex; align-items: center; gap: 8px; }
.mon-acc-score { font-weight: 700; font-family: var(--font-display); width: 30px; font-variant-numeric: tabular-nums; }
.mon-acc-gauge .gauge__track { flex: 1; }

/* 日志 */
.mon-log-list { display: flex; flex-direction: column; gap: 6px; max-height: 360px; overflow-y: auto; }
.mon-log-row { display: flex; align-items: center; gap: 9px; font-size: 12.5px; padding: 5px 0; border-bottom: 1px dashed var(--line); }
.mon-log-dot { width: 8px; height: 8px; border-radius: 50%; flex: 0 0 auto; }
.mon-log-type { flex: 0 0 auto; font-family: ui-monospace, Consolas, monospace; font-size: 11px; color: var(--ink-faint); min-width: 70px; }
.mon-log-msg { flex: 1; color: var(--ink-soft); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.mon-log-time { flex: 0 0 auto; font-size: 11px; color: var(--ink-faint); }

/* ═══════════════════════════════════════════════════════════
   通知中心（P4-A）· 铃铛 / 下拉面板 / 通知列表
   ═══════════════════════════════════════════════════════════ */

.bell-wrap { position: relative; }

.bell-btn {
  position: relative;
  display: inline-flex; align-items: center; justify-content: center;
  width: 36px; height: 36px;
  border: 1px solid var(--line);
  border-radius: var(--radius);
  background: var(--card);
  color: var(--ink-soft);
  cursor: pointer;
  transition: background .15s, color .15s, border-color .15s;
}
.bell-btn:hover { background: var(--paper-deep); color: var(--ink); }
.bell-btn .ico { color: var(--ink-soft); }

.bell-badge {
  position: absolute; top: -5px; right: -5px;
  min-width: 17px; height: 17px; padding: 0 4px;
  border-radius: 9px;
  background: var(--danger); color: #fff;
  font-size: 11px; font-weight: 700; line-height: 17px;
  text-align: center;
  box-shadow: 0 0 0 2px var(--card);
}

/* 下拉面板 */
.bell-panel {
  position: absolute; top: calc(100% + 10px); right: 0;
  width: 340px; max-height: 460px;
  display: none; flex-direction: column;
  background: var(--card);
  border: 1px solid var(--line);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-2);
  z-index: 90;
  overflow: hidden;
}
.bell-panel--open { display: flex; }
.bell-panel__head {
  padding: 12px 16px;
  font-weight: 600; color: var(--ink);
  border-bottom: 1px solid var(--line);
}
.bell-panel__list { overflow-y: auto; flex: 1; }
.bell-panel__loading, .bell-panel__empty {
  padding: 28px 16px; text-align: center; color: var(--ink-faint); font-size: 13px;
}
.bell-panel__empty .ico { margin: 0 auto 8px; color: var(--line-strong); }
.bell-panel__foot {
  display: flex; justify-content: space-between; align-items: center;
  padding: 8px 12px; border-top: 1px solid var(--line);
}

.bell-item {
  display: flex; gap: 10px; align-items: flex-start;
  padding: 10px 16px; cursor: pointer;
  border-bottom: 1px solid var(--paper-deep);
}
.bell-item:hover { background: var(--brand-tint-2); }
.bell-item--unread { background: var(--brand-tint-2); }
.bell-item__dot {
  flex: none; width: 8px; height: 8px; margin-top: 6px;
  border-radius: 50%; background: var(--brand);
  visibility: hidden;
}
.bell-item--unread .bell-item__dot { visibility: visible; }
.bell-item__body { flex: 1; min-width: 0; }
.bell-item__top { display: flex; justify-content: space-between; gap: 8px; }
.bell-item__top b { font-size: 13px; color: var(--ink); }
.bell-item__time { flex: none; font-size: 11px; color: var(--ink-faint); }
.bell-item__content {
  font-size: 12.5px; color: var(--ink-soft);
  overflow: hidden; text-overflow: ellipsis;
  display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical;
}

/* 通知中心页面 */
.notif-tabs { display: flex; flex-wrap: wrap; gap: 8px; padding: 12px 16px; border-bottom: 1px solid var(--line); }
.notif-tab {
  border: 1px solid var(--line); background: var(--card);
  color: var(--ink-soft); font-size: 13px;
  padding: 4px 12px; border-radius: 999px; cursor: pointer;
  transition: all .15s;
}
.notif-tab:hover { border-color: var(--line-strong); }
.notif-tab--active { background: var(--brand); border-color: var(--brand); color: #fff; }

.notif-list { padding: 8px 0; }
.notif-item {
  display: flex; gap: 12px; align-items: flex-start;
  padding: 14px 16px; cursor: pointer;
  border-bottom: 1px solid var(--paper-deep);
}
.notif-item:hover { background: var(--brand-tint-2); }
.notif-item--unread { background: var(--brand-tint-2); }
.notif-item__tag {
  flex: none; margin-top: 2px;
  font-size: 11px; font-weight: 600;
  padding: 2px 8px; border-radius: 6px;
  background: var(--paper-deep); color: var(--ink-soft);
}
.notif-item--info .notif-item__tag { background: var(--info-tint); color: var(--info); }
.notif-item--warning .notif-item__tag { background: var(--warn-tint); color: var(--warn); }
.notif-item--error .notif-item__tag { background: var(--danger-tint); color: var(--danger); }
.notif-item--success .notif-item__tag { background: var(--ok-tint); color: var(--ok); }
.notif-item__body { flex: 1; min-width: 0; }
.notif-item__top { display: flex; justify-content: space-between; gap: 10px; align-items: baseline; }
.notif-item__top b { font-size: 14px; color: var(--ink); }
.notif-item--unread .notif-item__top b::before {
  content: ''; display: inline-block; width: 7px; height: 7px;
  border-radius: 50%; background: var(--brand); margin-right: 7px;
}
.notif-item__time { flex: none; font-size: 12px; color: var(--ink-faint); }
.notif-item__content { font-size: 13px; color: var(--ink-soft); margin-top: 2px; }
/* ═══════════════════════════════════════════════════════════
   P4-D 私信发送执行器 · 发送控制区
   ═══════════════════════════════════════════════════════════ */
.dm-sender__mockbar {
  background: var(--warn-tint);
  border: 1px solid var(--line-strong);
  color: var(--warn);
  padding: 10px 14px;
  border-radius: var(--radius-sm);
  font-size: 13px;
  font-weight: 600;
  margin-bottom: 14px;
}
.dm-sender__mockbar--cdp {
  background: var(--info-tint);
  color: var(--info);
  border-color: var(--line-strong);
}
.dm-sender__row {
  display: flex;
  flex-wrap: wrap;
  gap: 14px;
  align-items: flex-end;
  margin-bottom: 16px;
}
.dm-sender__field {
  display: flex;
  flex-direction: column;
  gap: 4px;
  min-width: 120px;
}
.dm-sender__field label {
  font-size: 12px;
  color: var(--ink-soft);
  font-weight: 600;
}
.dm-sender__field select,
.dm-sender__field input {
  height: 34px;
  padding: 0 10px;
  border: 1px solid var(--line-strong);
  border-radius: var(--radius-sm);
  background: var(--card);
  color: var(--ink);
  font-size: 13px;
}
.dm-sender__field--btn { min-width: auto; }
.dm-sender__run {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  align-items: center;
  margin-bottom: 12px;
}
.dm-sender__prog {
  margin-left: auto;
  font-size: 13px;
  color: var(--ink-soft);
  font-weight: 600;
}
.dm-sender__bar {
  height: 8px;
  background: var(--paper-deep);
  border-radius: 999px;
  overflow: hidden;
  margin-bottom: 12px;
}
.dm-sender__bar-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--brand), var(--brand-deep));
  width: 0%;
  transition: width .4s var(--ease);
}
.dm-sender__counts {
  display: flex;
  gap: 16px;
  flex-wrap: wrap;
}
.dm-count {
  font-size: 13px;
  color: var(--ink-soft);
}
.dm-count b { font-size: 15px; margin-left: 2px; }
.dm-count--ok b { color: var(--ok); }
.dm-count--fail b { color: var(--danger); }
.dm-count--thr b { color: var(--warn); }

/* 发送记录状态标记 */
.dm-status {
  display: inline-block;
  padding: 2px 10px;
  border-radius: 999px;
  font-size: 12px;
  font-weight: 600;
  white-space: nowrap;
}
.dm-status--success { background: var(--ok-tint); color: var(--ok); }
.dm-status--failed { background: var(--danger-tint); color: var(--danger); }
.dm-status--throttled { background: var(--warn-tint); color: var(--warn); }
.dm-status--need_captcha,
.dm-status--account_banned { background: var(--info-tint); color: var(--info); }


/* ══════════ Tab 组件（互动中心/账号管理/数据报表/系统设置） ══════════ */
.tabs { display: flex; gap: 4px; margin-bottom: 16px; border-bottom: 1px solid var(--border); }
.tab { padding: 8px 16px; border: none; background: none; cursor: pointer; font-size: 14px; color: var(--text-muted); border-bottom: 2px solid transparent; margin-bottom: -1px; }
.tab--active { color: var(--brand-deep); border-bottom-color: var(--brand-deep); font-weight: 600; }
.tab:hover { color: var(--text); }
