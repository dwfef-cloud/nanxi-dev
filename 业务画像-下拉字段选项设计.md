# 业务画像 · 下拉选项字段设计（已定稿并实施）

> 目标：把「填空」改成「下拉选 + 支持自定义」，让同一类信息在全站统一（业务画像 / 产品知识库 / 目标客户 / 微信转化 / 话术变量共用同一套选项）。
> 现状：25 个字段里 20 个是自由文本输入框（其中 9 个是多行）。自由文本导致同一件事有 N 种写法（"装修/家装/室内装修"），下游话术、筛选、AI 生成都没法稳定引用。

**实施状态（2026-09-13 已完成）**
- 落地：22 个字段用「单选下拉 / 多选标签」，3 个保留纯文本（微信号、产品简介、FAQ）。
- 交互：自研下拉（`.bdrop`），点开是自定义列表 + 顶部自定义输入框；多选字段已选为胶囊（× 移除），有胶囊时额外显示「＋ 选择」入口。
- 选项库：`app.js` 的 `BIZ_OPTIONS`，18 个 key，供全站引用。
- 存储：仍是单个字符串，**后端 schema / 模型 / 表零改动**；分隔符沿用各字段原有约定（关键词逗号、卖点/痛点换行、区域/语气顿号）。
- 已验证：25 个字段框高统一 60px；22 个下拉/标签选项全部非空；行业→主营产品联动；自定义输入；多选上限；保存→重载回填；0 页面错误。

---

## 一、字段盘点：哪些改下拉、哪些必须留文本

判定标准：
- **能枚举** → 下拉（预设选项 + 自定义输入）
- **唯一/自由表达** → 留文本（微信号、产品简介、FAQ 属此类）
- **多值**（一行一条/逗号分隔）→ 标签式多选（点预设 + 输入回车添加）

| # | 卡片 | 字段（key） | 现状 | 建议控件 | 是否改后端 |
|---|---|---|---|---|---|
| 1 | 业务画像 | 所属行业 `industry` | 单行 | **单选下拉 + 自定义** | 否 |
| 2 | 业务画像 | 主营产品 `product` | 单行 | **单选下拉 + 自定义**（选项随行业联动） | 否 |
| 3 | 业务画像 | 服务区域 `serviceArea` | 单行 | **多选标签 + 自定义** | 否 |
| 4 | 业务画像 | 目标客户 `targetCustomer` | 单行 | **单选下拉 + 自定义** | 否 |
| 5 | 业务画像 | 价格区间 `priceRange` | 单行 | **单选下拉 + 自定义** | 否 |
| 6 | 业务画像 | 转化目标 `conversionGoal` | 单行 | **单选下拉 + 自定义** | 否 |
| 7 | 业务画像 | 沟通语气 `tone` | 单行 | **多选标签（≤2 个）** | 否 |
| 8 | 产品知识库 | 产品名称 `productName` | 单行 | **单选下拉 + 自定义**（与 ②同源） | 否 |
| 9 | 产品知识库 | 产品简介 `description` | 多行 | ❌ 留文本 | 否 |
| 10 | 产品知识库 | 核心卖点 `sellingPoints` | 多行 | **多选标签 + 自定义** | 否 |
| 11 | 产品知识库 | 目标客户群 `targetCustomers` | 多行 | **多选标签 + 自定义**（复用④） | 否 |
| 12 | 产品知识库 | 价格区间 `priceRange` | 单行 | **单选下拉**（复用⑤同档位） | 否 |
| 13 | 产品知识库 | 常见问题 `faq` | 多行 | ❌ 留文本（给引导模板） | 否 |
| 14 | 产品知识库 | 禁止承诺 `forbiddenClaims` | 多行 | **多选标签 + 自定义**（合规黑名单） | 否 |
| 15 | 目标客户 | 客户群名称 `name` | 单行 | **单选下拉 + 自定义**（模板名） | 否 |
| 16 | 目标客户 | 所属行业 `industry` | 单行 | **单选下拉**（复用①） | 否 |
| 17 | 目标客户 | 所在区域 `region` | 单行 | **单选下拉**（复用③） | 否 |
| 18 | 目标客户 | 核心需求 `needs` | 多行 | **多选标签 + 自定义** | 否 |
| 19 | 目标客户 | 主要痛点 `painPoints` | 多行 | **多选标签 + 自定义** | 否 |
| 20 | 目标客户 | 意向关键词 `intentKeywords` | 逗号分隔 | **多选标签 + 自定义** | 否 |
| 21 | 目标客户 | 排除关键词 `excludedKeywords` | 逗号分隔 | **多选标签 + 自定义** | 否 |
| 22 | 微信转化 | 微信号 `wechatId` | 单行 | ❌ 留文本（唯一值） | 否 |
| 23 | 微信转化 | 引导加微时机 `guideTiming` | 单行 | **单选下拉 + 自定义** | 否 |
| 24 | 微信转化 | 引导理由 `guideReason` | 单行 | **多选标签 + 自定义** | 否 |
| 25 | 微信转化 | 合规备注 `complianceNote` | 多行 | **多选标签 + 自定义**（合规模板） | 否 |
| — | 话术策略 | 4 段文本 | 多行 | ❌ 不涉及（已迁到话术库） | 否 |

**结论：20 个字段改下拉/标签式，5 个保持文本（微信号、产品简介、FAQ、话术 4 段）。**

---

## 二、选项清单（预设值）

> 原则：先覆盖"本地生活服务/专业服务"这类抖音获客主战场；每题都带**「自定义…」**，选后即变输入框，不锁死用户。

### ① 所属行业 `industry`（单选，24 项）
| 分组 | 选项 |
|---|---|
| 本地生活 | 装修/家装 · 建材/家居 · 搬家/保洁 · 家电/家居维修 · 餐饮/小吃 · 宠物服务 · 婚庆/摄影 · 旅游/民宿 |
| 健康美业 | 美容/美发/美甲 · 医美/整形 · 健身/瑜伽 · 口腔/牙科 · 健康体检/中医 · 月子/母婴 |
| 专业服务 | 房产/中介 · 法律咨询 · 财税代账 · 保险/金融 · 教育培训 · 汽车销售 · 汽车服务/维修 |
| 商业/线上 | 软件/小程序/代运营 · 批发/零售 · 其他 |

### ② 主营产品 `product`（单选，**随行业过滤**，例：装修/家装）
半包套餐 · 全包套餐 · 整装套餐 · 局部翻新 · 旧房改造 · 软装设计 · 免费量房出方案 · 其他
> 通用兜底（未匹配行业时）：服务套餐 · 单品/整机 · 会员/年卡 · 课程/培训 · 咨询/方案 · 定制服务

### ③ 服务区域 `serviceArea`（**多选**，7 项）
同城（本市） · 本市主城区 · 本市全境 · 周边区县 · 跨市/邻省 · 全国（可线上交付） · 指定城市（自定义）

### ④ 目标客户 `targetCustomer`（单选，10 项）
个人消费者 · 家庭用户 · 年轻白领 · 宝妈/家庭主妇 · 业主/房东 · 企业/公司采购 · 个体商户/门店 · 政府/事业单位 · 学生 · 老年人群

### ⑤ 价格区间 `priceRange`（单选，通用档位 11 项 · 低档加密版）
面议/按方案报价 · 500 元以内 · 500–2000 元 · 2000–5000 元 · 5000–1 万 · 1–3 万 · 3–5 万 · 5–10 万 · 10–30 万 · 30 万以上 · 按月/年订阅

> 相比初稿的调整：删掉「100 万以上」并把「30–100 万 / 100 万以上」合并为「30 万以上」；把原来的「1000 元以内 / 1000–5000」拆成 **500 以内 / 500–2000 / 2000–5000** 三档。
> 原因：抖音获客的成交大头集中在小额服务与咨询，原档位从 1000 元起跳、颗粒太粗，选不中自己那档。
> 若你的客单价主要在千元级（如 199–2999 的服务/诊断类），可再把 1 万以上合并成「1–10 万 / 10 万以上」两档，把省下的位置继续加密千元段。

### ⑥ 转化目标 `conversionGoal`（单选，9 项）
加微信/企业微信 · 留手机号 · 私信回复 · 到店/上门 · 直接下单 · 预约体验 · 关注账号 · 进群 · 领取资料

### ⑦ 沟通语气 `tone`（**多选 ≤2**，8 项）
专业严谨 · 真诚亲切 · 热情有活力 · 干脆利落 · 耐心细致 · 幽默轻松 · 高端克制 · 接地气/本地化

### ⑩ 核心卖点 `sellingPoints`（多选，10 + 自定义）
免费上门量房/评估 · 价格透明不增项 · 自有团队不转包 · 材料品牌可选 · 多年质保 · 先看案例再决定 · 不满意可返工 · 一对一专属顾问 · 同城当天响应 · 签订正规合同

### ⑪ 目标客户群 `targetCustomers`（多选，复用④的 10 项）

### ⑭ 禁止承诺 `forbiddenClaims`（多选合规黑名单，8 项）
保证零增项 · 全市/全网最低价 · 绝对/100% 保证 · 包治/根治 · 官方指定/唯一授权 · 永久免费 · 当天见效 · 无任何风险

### ⑮ 客户群名称 `name`（单选 + 自定义，模板 3 项）
主城毛坯房业主 · 旧房改造客户 · 同城有需求客户
> 也可二期做「行业 + 区域」自动生成建议名。

### ⑱ 核心需求 `needs`（多选，9 项）
想了解报价 · 想对比方案 · 想先看案例 · 想确认工期 · 想了解材料/用料 · 想确认售后保障 · 想上门量房/看现场 · 想省钱/找优惠 · 想了解资质

### ⑲ 主要痛点 `painPoints`（多选，10 项）
怕增项加价 · 怕工期拖延 · 怕偷工减料 · 怕材料以次充好 · 怕售后没人管 · 不懂行怕被坑 · 预算不够 · 选择困难 · 担心效果不符 · 怕交定金有风险

### ⑳ 意向关键词 `intentKeywords`（多选，11 项）
多少钱 · 报价 · 怎么收费 · 贵不贵 · 有优惠吗 · 怎么联系 · 能上门吗 · 地址在哪 · 想了解一下 · 加个微信 · 什么时候能看

### ㉑ 排除关键词 `excludedKeywords`（多选，10 项）
招聘 · 求职 · 兼职 · 实习 · 学徒 · 加盟 · 代理 · 批发 · 同行 · 免费

### ㉓ 引导加微时机 `guideTiming`（单选，7 项）
评论区互动 1 次后 · 私信咨询回复后 · 发送报价后 · 发送案例后 · 用户主动问价时 · 连续互动 2 次以上 · 留资后 24 小时内

### ㉔ 引导理由 `guideReason`（多选，7 项）
发详细报价单 · 发同小区/同类案例 · 领避坑清单或资料包 · 免费上门量尺/评估 · 专属顾问 1 对 1 · 进客户交流群 · 领专属优惠

### ㉕ 合规备注 `complianceNote`（多选，6 项）
不主动索要手机号 · 不承诺保价/最低价 · 不做绝对化承诺 · 不诱导私下交易 · 不群发骚扰 · 不冒充官方账号

---

## 三、交互方案（两选一）

### 方案 A：原生 `datalist` —— 改动最小（推荐先用）
- 单行字段由 `<input type="text">` 改为 `<input list="...">` + `<datalist>`：**点开是下拉候选，也能直接打任意文字**，天然满足"选项 + 自定义"。
- 落地范围：只改 `app.js` 的 `fieldHtml(f, val)` 一个函数 + 新增一份选项常量，**9 个字段一次到位**。
- 缺点：下拉样式跟随浏览器（不可定制）、Safari 无下拉箭头（可接受）。

### 方案 B：自研下拉面板 —— 样式统一可控
- 复用现有的 `.tp-drop` 面板模式：点开是自定义列表，最后一项「+ 自定义…」切换成输入框。
- 优点：与站内其他下拉观感一致，可显示分组/说明；**多值字段本来就必须自研标签输入，可共用同一套组件**。
- 缺点：代码量约为方案 A 的 3 倍，需要处理键盘、失焦、遮挡。

**多值字段（9 个）两种方案都要做"标签输入"**：预设标签点击即选中，输入框回车添加自定义值，选中项以胶囊显示、可点 × 删除。存储仍是一个字符串（换行或 `、` 分隔），**后端 schema / 模型 / 表一律不动**。

---

## 四、统一性约定（这次改造的核心价值）

1. **一份选项库 `BIZ_OPTIONS`**：行业 / 区域 / 价格档位 / 目标客户 四套库定义一次，"业务画像 · 产品知识库 · 目标客户 · 话术变量"全部引用同一份 → 同一个概念全站只有一种写法。
2. **去重复字段**：`主营产品`（业务画像）与 `产品名称`（产品知识库）是同一个东西，选项同源；建议保存时互相同步，避免两处填不同值。
3. **为下游铺路**：`行业` 选了之后，`主营产品`、`客户群名称`、话术变量里的 `{行业}` 都跟着统一；后续做"按行业推荐话术模板""按关键词筛选线索"才有稳定依据。
4. **不锁死**：每个字段都留自定义入口，选项库只做"常用集合"，不是白名单校验。

---

## 五、实施顺序（确认后开工）

1. 新增 `BIZ_OPTIONS` 常量（选项库）→ `app.js`
2. 改 `fieldHtml`：支持 `f.options`（单选）与 `f.multiSel`（多选标签）
3. `BIZ_FIELDS` 逐字段挂 `options` / `multiSel`
4. 多值字段做标签输入（含自定义回车添加、× 删除）
5. 保存/回填沿用现有 `BIZ_SAVERS`（多值 join 成字符串，后端不变）
6. 截图验证五档宽度 + 保存往返 + 空值状态

---

## 六、定稿决策（2026-09-13 确认）

1. **行业库**：24 项够用，不改。
2. **价格档位**：采用「重心下移版」——面议 / 500 元以内 / 500–2000 / 2000–5000 / 5000–1 万 / 1–3 万 / 3–5 万 / 5–10 万 / 10–30 万 / 30 万以上 / 按月·年订阅。
3. **服务区域**：多选（可同时选"本市主城区 + 周边区县"）。
4. **交互方案**：方案 B · 自研下拉（样式统一可控）。

### 实施落点（供后续维护）
| 位置 | 内容 |
|---|---|
| `app.js` `BIZ_OPTIONS` | 选项库（18 个 key：industry / productGeneric / productByIndustry / area / target / price / goal / tone / groupName / sellingPoints / forbidden / needs / pain / intentKw / excludedKw / timing / guideReason / compliance） |
| `app.js` `BIZ_FIELDS` | 每字段挂 `sel: '<key>'`（单选）或 `tags: '<key>'`+`tagSep`（多选，tagSep 决定存储分隔符）或 `multi: true`（多行文本） |
| `app.js` `fieldHtml()` | 三种控件的 HTML；下拉结构 `.bdrop > .bdrop__box + .bdrop__panel`，真实值存在 `input[type=hidden][data-field]` |
| `app.js` `wireFields()` | 展开/选择/自定义回车/胶囊 ×/上限拦截/点外收起；展开时按表单当前值实时算选项（行业联动靠这里） |
| `app.js` `resolveOpts()` | 取选项；`product` 特殊：随「所属行业」联动，未匹配用 `productGeneric` 兜底 |
| `style.css` `.bdrop__*` / `.bchip` | 触发框 60px、面板向上/向下自适应、胶囊样式 |

**未做但建议**：`主营产品` 与 `产品名称` 是同源字段，目前只做到"选项同源"，没有保存时互相写入（避免覆盖用户已填内容）。
