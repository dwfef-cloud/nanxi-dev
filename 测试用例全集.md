# 获客系统 · 全系统测试用例全集

> 版本：v1.0-S3 · 编写日期：2026-09-12
> 范围：前端全模块 + 后端全接口（人读可执行，逐条勾选）
> 环境：后端 `127.0.0.1:8000`（FastAPI/SQLite）· 前端 `127.0.0.1:8081` · 采集服务 `127.0.0.1:8090`（MediaCrawler）

---

## 一、执行说明

### 1.1 服务启动（每次执行前确认）
```bash
# 后端（必须用含 uvicorn 的 3.13.12.old.25308）
cd /d/nanxi-dev/backend && "C:/Users/天选Air/.workbuddy/binaries/python/versions/3.13.12.old.25308/python.exe" -m uvicorn app.main:app --host 127.0.0.1 --port 8000
# 前端
cd /d/nanxi-dev/frontend && "C:/Users/天选Air/.workbuddy/binaries/python/versions/3.13.12/python.exe" -m http.server 8081 --bind 127.0.0.1
# 8090 由后端 lifespan 自动拉起；或手动 POST /api/crawler/service/start
```
就绪校验：`GET /api/onboarding/readiness` → `backend.ok=true` 且 `crawl_service.ok=true`。

### 1.2 用例字段说明
- **编号**：模块前缀 + 序号（如 `TC-ACC-001`）
- **优先级**：P0 核心链路 / P1 重要功能 / P2 边缘场景
- **类型**：UI（界面）· API（接口）· E2E（端到端）· ERR（异常边界）
- **执行结果**：`[ ] 通过  [ ] 失败  [ ] 阻塞`，失败需填「实际现象」

### 1.3 全局前置
- 浏览器访问 `http://127.0.0.1:8081/`，打开 DevTools Console 观察是否有报错
- 抖音扫码登录需真人手机确认（自动化无法代劳）

---

## 二、环境 / 启动（TC-ENV）

### TC-ENV-001 三服务端口就绪【P0·UI】
- 前置：按 1.1 启动
- 步骤：
  1. 打开前端首页，地址栏 `#/workbench`
  2. 调用 `/api/onboarding/readiness`
- 预期：8000/8081/8090 均 OPEN；readiness `backend.ok=true`、`crawl_service.ok=true`；首页无白屏

### TC-ENV-002 未登录态引导【P0·UI】
- 步骤：直接访问 `/api/onboarding/readiness`
- 预期：返回 `douyin_login.ok=false`，`blocking` 含 `"douyin_login"`，`action.type="goto_step"`、`step=2`（去扫码登录）

### TC-ENV-003 安全模式横幅【P1·UI】
- 步骤：触发安全模式后刷新页面
- 预期：顶部出现红色 `#safe-mode-banner`，显示原因与「人工确认退出」按钮

### TC-ENV-004 顶部状态栏【P1·UI】
- 步骤：观察 `#topbar-status`
- 预期：显示「采集运行中/空闲」pill 与「安全模式：开/关」pill，状态随后端变化

---

## 三、工作台（TC-WB · #/workbench）

### TC-WB-001 工作台加载【P0·UI】
- 预期：漏斗图、北极星指标（`#north-star-value`）、各模块入口卡片正常渲染，Console 无报错

### TC-WB-002 北极星指标展示【P1·UI】
- 步骤：查看左侧 `#north-star-value`
- 预期：显示加企微数（默认示例 41）与本周增量

### TC-WB-003 一键暂停【P0·UI】
- 步骤：点「一键暂停」→ 确认
- 预期：调用 `POST /compliance/pause`；顶部 pill 变「已暂停」；全部新触达暂停

### TC-WB-004 通知中心入口【P1·UI】
- 步骤：点铃铛 `#btn-notifications`
- 预期：展开 `#bell-panel`，显示未读通知；红点 `#bell-badge` 计数正确

### TC-WB-005 配置按钮【P1·UI】
- 步骤：点「配置」#btn-settings
- 预期：打开系统设置弹窗/路由

---

## 四、采集评论（TC-CRAWL · #/crawl）

### TC-CRAWL-001 未登录跳登录【P0·UI】★近期改动
- 前置：抖音未登录（`douyin_login.ok=false`）
- 步骤：
  1. 进入 `#/crawl`
  2. 点「启动采集」
- 预期：弹出黄色 `#crawl-login-banner` 引导横幅；点「去扫码登录」→ 跳转 `#/accounts?tab=wizard&step=login`

### TC-CRAWL-002 已登录启动采集【P0·UI】
- 前置：已扫码登录
- 步骤：点「启动采集」填写关键词/视频链接 → 确认
- 预期：调用 `POST /api/crawler/live/start`；状态轮询 `GET /api/crawler/live/status`；顶部 pill 变「采集运行中」

### TC-CRAWL-003 采集状态轮询【P1·UI】
- 步骤：采集中点状态区
- 预期：`live/status` 返回 `running`，可查看 `live/logs`；停止后 `idle`

### TC-CRAWL-004 采集任务列表（CrawlTask 体系）【P1·API】
- 步骤：`GET /api/crawl/tasks`
- 预期：返回**顶层数组**（非 `{items}`）；字段含 `id/keyword/status`

### TC-CRAWL-005 创建采集任务【P1·API】
- 步骤：`POST /api/crawl/tasks` `{keyword, source}`
- 预期：201，返回任务对象

### TC-CRAWL-006 启动/停止任务【P1·API】
- 步骤：`POST /api/crawl/tasks/{id}/start` 与 `/stop`
- 预期：状态在 `pending→running→stopped` 间流转；`GET /tasks/{id}/status` 一致

### TC-CRAWL-007 导入采集结果【P1·API】
- 步骤：`POST /api/crawl/import`
- 预期：外部数据写入 leads/comments 库，返回导入条数

### TC-CRAWL-008 采集服务启停【P1·API】
- 步骤：`GET /api/crawler/service` → `POST /service/start` → `POST /service/stop`
- 预期：service 状态 `idle/running` 切换；8090 可达性随之变化

### TC-CRAWL-009 子评论开关【P2·API】
- 前置：`enable_sub_comments` 未传
- 步骤：创建任务不传 `enable_sub_comments`
- 预期：后端默认置 `False`（回复检测依赖子评论，需显式开启）

### TC-CRAWL-010 demo/文件导入【P2·API】
- 步骤：`POST /api/crawler/import-demo`、`POST /api/crawler/import-file`
- 预期：分别导入演示数据/本地文件，返回任务

---

## 五、评论候选池（TC-CG · #/comment-growth）

### TC-CG-001 候选池加载【P0·UI】
- 前置：存在 own_comment 且有原文+地址可用的 lead
- 步骤：进入 `#/comment-growth`
- 预期：候选池读 `leads`（own_comment+原文+地址可用+未建任务），非 `comment_tasks`

### TC-CG-002 确认回复真发抖音【P0·UI】★关键设计
- 步骤：选评论 → 写回复 → 「确认回复」
- 预期：走唯一出口 `runSend(payload, confirm)`；经 9222 浏览器自动化真实发送；发送前若地址全不可用即时 400

### TC-CG-003 地址兜底【P1·UI】
- 步骤：lead 缺 `source_url` 时检查
- 预期：`app/core/douyin_url.py::usable_video_url` 统一判定兜底；全部不可用则发送拦截

### TC-CG-004 评论执行预览【P1·API】
- 前置：构造 `{match, author, text}`
- 步骤：`POST /api/comments/execute` `confirm=false`
- 预期：返回预览（不真发）；响应结构 `CommentExecutionResponse(**{**fields,**result})` 不重名

### TC-CG-005 评论执行真发【P0·API】
- 步骤：`POST /api/comments/execute` `confirm=true`
- 预期：真发成功；缺 `match+author` 时直接 400；命中 0 或多条时停手不发送

### TC-CG-006 代理状态查询【P1·API】
- 步骤：`GET /api/comments/agent/status`
- 预期：返回浏览器代理在线状态（区分 CrawlAPI.getTaskStatus 另一套）

### TC-CG-007 浏览器手动启动【P2·API】
- 步骤：`POST /api/comments/agent/browser/start`
- 预期：拉起 9222 Chrome 供发送

### TC-CG-008 选评论脚本【P2·API】
- 步骤：`GET /api/scripts/comment/pick`
- 预期：返回可回复的评论候选

---

## 六、待办互动（TC-INT · #/interact）

### TC-INT-001 互动队列加载【P0·UI】
- 步骤：进入 `#/interact`
- 预期：`GET /api/dm/inbox` 或 behavior 数据渲染；徽标 `#nav-interact-badge` 计数正确

### TC-INT-002 回复互动【P1·UI】
- 步骤：选一条 → 回复
- 预期：走 `dm_inbox` 回复接口，前端提示「私信发送为模拟（mock）」

### TC-INT-003 标记加企微【P1·API】
- 步骤：`POST /api/dm/inbox/{id}/mark-wechat`
- 预期：该会话标记「已加企微」，同步客户阶段

### TC-INT-004 模拟互动【P2·API】
- 步骤：`POST /api/dm/inbox/simulate`
- 预期：生成模拟互动数据供演示

### TC-INT-005 私信仍为 mock 提示【P0·UI】
- 预期：界面明确标注私信发送是模拟，不真实触达

---

## 七、话术库（TC-SCR · #/scripts）

### TC-SCR-001 话术列表【P0·UI】
- 步骤：进入 `#/scripts`
- 预期：展示分类话术（引流/跟进/成交/拒绝应对），可搜索

### TC-SCR-002 新建话术【P1·UI】
- 步骤：新建 → 填场景+内容 → 保存
- 预期：落库，列表刷新

### TC-SCR-003 话术被引用【P2·UI】
- 步骤：在评论候选池/客户跟进中调用话术
- 预期：可一键插入

### TC-SCR-004 话术编辑删除【P2·UI】
- 预期：编辑/删除生效，引用处提示失效

---

## 八、账号管理（TC-ACC · #/accounts）★含上线向导

### TC-ACC-001 账号列表（两 Tab）【P0·UI】★近期改动
- 步骤：进入 `#/accounts`
- 预期：仅「账号」+「上线向导」两 Tab（已合并）；账号表格全宽（非 720px 居中）

### TC-ACC-002 账号字段展示【P1·UI】
- 预期：行含昵称、状态、因子标签（`.acc-factor-tag`），紧凑布局

### TC-ACC-003 规则抽屉【P1·UI】★近期改动
- 步骤：点「查看规则」
- 预期：右侧抽屉 `.acc-rules-drawer` 展示获客规则，非独立页

### TC-ACC-004 新建/编辑账号【P1·API】
- 步骤：`POST /api/accounts` / `PATCH /api/accounts/{id}`
- 预期：返回 `AccountRead`；响应用别名（response_model_by_alias）

### TC-ACC-005 上线向导全宽【P0·UI】★近期改动
- 步骤：切到「上线向导」Tab
- 预期：向导页全宽（`.wizard{max-width:none}`），非小弹窗

### TC-ACC-006 业务配置全宽【P0·UI】★近期改动
- 步骤：向导内业务配置步骤
- 预期：业务配置区全宽；`GET/PUT /api/business/profile` 读写正常

### TC-ACC-007 扫码登录唤起抖音【P0·UI】★近期改动
- 前置：8090 正常、`CDP_CONNECT_EXISTING=False`
- 步骤：向导 → 扫码登录步骤 → 点「打开扫码登录」
- 预期：MediaCrawler 自动启 Chrome（9222），**浏览器停在抖音扫码页**（非 newtab）；前端轮询 `GET /api/crawler/live/status`（running→idle=成功）

### TC-ACC-008 登录后状态持久【P1·UI】
- 步骤：手机扫码确认 → 等待
- 预期：`/api/onboarding/readiness` 的 `douyin_login.ok` 变 true；横幅消失

### TC-ACC-009 深层链接直达登录【P1·UI】★近期改动
- 步骤：访问 `#/accounts?tab=wizard&step=login`
- 预期：直接定位到扫码登录步骤

### TC-ACC-010 调度面板绑定【P2·UI】
- 步骤：账号绑定调度
- 预期：`api-scheduler` 绑定生效，定时任务可见

---

## 九、我的客户（TC-CUS · #/customers）

### TC-CUS-001 客户列表【P0·UI】
- 步骤：进入 `#/customers`
- 预期：`GET /api/customers` 返回；表格含阶段、来源、加企微时间

### TC-CUS-002 客户详情抽屉【P1·UI】
- 步骤：点客户行
- 预期：`#lead-drawer` 弹出，展示资料/互动/阶段日志

### TC-CUS-003 新建客户【P1·API】
- 步骤：`POST /api/customers`
- 预期：201 返回客户

### TC-CUS-004 阶段推进【P1·API】
- 步骤：`POST /api/customers/{id}/stage` `{stage}`
- 预期：阶段变更，漏斗数据更新

### TC-CUS-005 成交录入【P0·API】
- 步骤：`POST /api/customers/{id}/deal` `{amount}`
- 预期：标记成交，金额入 analytics

### TC-CUS-006 丢单【P1·API】
- 步骤：`POST /api/customers/{id}/lost`
- 预期：标记流失，不计入成交

### TC-CUS-007 编辑/删除【P1·API】
- 步骤：`PATCH /api/customers/{id}`、`DELETE /api/customers/{id}`
- 预期：生效；删除后列表消失

### TC-CUS-008 客户日志【P1·API】
- 步骤：`GET /api/customers/{id}/logs`
- 预期：返回阶段/成交/互动时间线

### TC-CUS-009 阶段元信息【P2·API】
- 步骤：`GET /api/customers/stage-meta`
- 预期：返回阶段枚举与定义

### TC-CUS-010 列表筛选排序【P1·UI】
- 预期：按阶段/来源/时间筛选，结果正确

---

## 十、今日待跟进（TC-FU · #/followups）

### TC-FU-001 待跟进列表【P0·UI】
- 步骤：进入 `#/followups`
- 预期：`GET /api/followups` 返回今日待办；徽标 `#nav-fu-badge` 计数

### TC-FU-002 新建待跟进【P1·API】
- 步骤：`POST /api/followups`
- 预期：201 返回

### TC-FU-003 完成跟进【P1·UI】
- 步骤：标记完成
- 预期：从列表移除，关联客户阶段更新

### TC-FU-004 跳转客户【P1·UI】
- 步骤：点待跟进项
- 预期：跳 `#/customers` 并打开对应详情抽屉

### TC-FU-005 提醒时间【P2·UI】
- 预期：临近提醒时间有高亮/通知

---

## 十一、数据分析（TC-ANL · #/analytics）

### TC-ANL-001 报表加载【P0·UI】
- 步骤：进入 `#/analytics`
- 预期：`GET /api/analytics/report` 渲染图表

### TC-ANL-002 投放花费录入【P1·API】
- 步骤：`POST /api/analytics/spend` → `GET /api/analytics/spend`
- 预期：录入并回读

### TC-ANL-003 漏斗分析【P1·API】
- 步骤：`GET /api/analytics/funnel`
- 预期：返回公域→私域→成交漏斗各层数据

### TC-ANL-004 多维拆分【P1·API】
- 步骤：`GET /api/analytics/by-account|by-script|by-industry|by-source`
- 预期：分别按账号/话术/行业/来源拆分

### TC-ANL-005 看板接口【P1·API】
- 步骤：`GET /dashboard/funnel|attribution|compliance|runtime`
- 预期：返回对应聚合数据

### TC-ANL-006 数据导出【P1·API】
- 步骤：`GET /api/analytics/export`
- 预期：返回可导出的数据集/文件

### TC-ANL-007 导出线索/客户/成交【P1·API】
- 步骤：`GET /api/export/leads|customers|deals`
- 预期：分别返回 CSV/JSON 下载

---

## 十二、系统监控（TC-MON · #/monitor）

### TC-MON-001 监控页加载【P0·UI】
- 步骤：进入 `#/monitor`
- 预期：`api-monitor` 渲染服务健康/资源

### TC-MON-002 服务健康【P1·API】
- 预期：后端/采集服务/前端状态可视化，异常标红

### TC-MON-003 运行时指标【P1·API】
- 步骤：`GET /dashboard/runtime`
- 预期：返回运行时间、任务数、队列长度

### TC-MON-004 采集日志【P1·API】
- 步骤：`GET /api/crawler/live/logs`
- 预期：返回实时采集日志

### TC-MON-005 告警展示【P2·UI】
- 预期：异常指标触发告警条

---

## 十三、合规风险（TC-RISK · #/risk）

### TC-RISK-001 合规页加载【P0·UI】
- 步骤：进入 `#/risk`
- 预期：展示护栏规则、安全模式状态

### TC-RISK-002 安全模式进入【P0·API】
- 步骤：`POST /compliance/safe-mode/enter` `{reason}`
- 预期：全部新触达暂停；前端横幅出现

### TC-RISK-003 安全模式退出【P0·API】
- 步骤：`POST /compliance/safe-mode/exit`
- 预期：恢复触达；横幅消失

### TC-RISK-004 一键暂停/恢复【P0·API】
- 步骤：`POST /compliance/pause` / `/resume`
- 预期：触达暂停/恢复，状态栏同步

### TC-RISK-005 评论护栏规则【P1·API】
- 步骤：`GET /api/comments/guard-rules` → `POST` 新增 → `PATCH` 启用
- 预期：规则 CRUD 生效

### TC-RISK-006 护栏校验【P1·API】
- 步骤：`POST /api/comments/guard-check` `{text}`
- 预期：返回是否命中违禁词/风险

### TC-RISK-007 脱敏【P1·API】
- 步骤：`POST /api/comments/desensitize` `{text}`
- 预期：手机号/微信等敏感信息被脱敏

### TC-RISK-008 合规看板【P1·API】
- 步骤：`GET /dashboard/compliance`
- 预期：返回合规指标（暂停率、拦截数）

---

## 十四、系统设置（TC-SET · #/settings）

### TC-SET-001 设置页加载【P0·UI】
- 步骤：进入 `#/settings`
- 预期：展示业务配置/AI/备份等分组入口

### TC-SET-002 业务配置读写【P0·API】
- 步骤：`GET/PUT /api/business/profile`
- 预期：返回/更新业务画像（注意字段 snake，勿 `_normalize`）

### TC-SET-003 AI 设置【P1·API】
- 步骤：`GET/PUT /api/ai/settings`
- 预期：返回/保存 AI 配置（未配置时 readiness 的 `ai.ok=false`）

### TC-SET-004 发送配置【P1·API】
- 步骤：`GET/PUT /api/dm/sender/config`
- 预期：私信发送参数读写

### TC-SET-005 策略开关【P1·API】
- 步骤：`api-settings` 的 `fetchStrategy*` 系列
- 预期：读取字段为 `r1_enabled` 等 snake 命名

### TC-SET-006 设置持久化【P1·UI】
- 预期：刷新后设置保留

---

## 十五、数据备份（TC-BAK · #/backup）★已全宽

### TC-BAK-001 备份页全宽【P0·UI】★近期改动
- 步骤：进入备份页
- 预期：`.backup-page{max-width:none}` 全宽（非 960px）

### TC-BAK-002 创建备份【P0·API】
- 步骤：`POST /api/backup/create`
- 预期：返回 `BackupInfo`，生成备份文件

### TC-BAK-003 备份列表【P1·API】
- 步骤：`GET /api/backup/list`
- 预期：返回 `BackupListResponse`

### TC-BAK-004 备份设置【P1·API】
- 步骤：`GET/PUT /api/backup/settings`
- 预期：自动备份策略读写

### TC-BAK-005 导出 JSON【P1·API】
- 步骤：`POST /api/backup/export-json`
- 预期：返回 `ExportResponse` 下载链接

### TC-BAK-006 恢复预览【P1·API】
- 步骤：`POST /api/backup/{filename}/restore`
- 预期：返回恢复预览（未生效）

### TC-BAK-007 恢复确认【P0·API】
- 步骤：`POST /api/backup/{filename}/restore/confirm`
- 预期：数据恢复生效

### TC-BAK-008 删除备份【P1·API】
- 步骤：`DELETE /api/backup/{filename}`
- 预期：返回 `DeleteResponse`，文件移除

### TC-BAK-009 备份失败兜底【P2·ERR】
- 预期：磁盘满/文件锁时返回明确错误，不破坏现库

---

## 十六、私信发送（TC-DM · dm_sender）★仍为 mock

### TC-DM-001 单发【P1·API】
- 步骤：`POST /api/dm/send/{lead_id}`
- 预期：返回 `SendResultOut`；界面注明为模拟

### TC-DM-002 批量发送【P1·API】
- 步骤：`POST /api/dm/send-batch`
- 预期：返回 `BatchSendResponse`，可查 `GET /send-batch/status`

### TC-DM-003 批量停止【P1·API】
- 步骤：`POST /api/dm/send-batch/stop`
- 预期：批量任务停止

### TC-DM-004 发送结果【P1·API】
- 步骤：`GET /api/dm/send-results`
- 预期：返回发送记录列表

### TC-DM-005 失败重试【P1·API】
- 步骤：`POST /api/dm/retry-failed`
- 预期：重试失败项

### TC-DM-006 发送测试【P2·API】
- 步骤：`POST /api/dm/sender/test`
- 预期：测试发送返回结果

### TC-DM-007 发送配置【P1·API】
- 步骤：`GET/PUT /api/dm/sender/config`
- 预期：发送参数读写

### TC-DM-008 mock 明确提示【P0·UI】
- 预期：私信相关界面显著标注「当前为模拟发送，未真实触达」

---

## 十七、私信收件箱（TC-INB · dm_inbox）

### TC-INB-001 收件箱列表【P0·UI】
- 步骤：进入互动/收件箱
- 预期：`GET /api/dm/inbox` 返回顶层数组；字段含会话摘要

### TC-INB-002 会话详情【P1·API】
- 步骤：`GET /api/dm/inbox/{id}`
- 预期：返回 `InboxConversationDetail`

### TC-INB-003 回复【P1·API】
- 步骤：`POST /api/dm/inbox/{id}/reply`
- 预期：返回 `InboxMessageItem`，追加到会话

### TC-INB-004 标记加企微【P1·API】
- 步骤：`POST /api/dm/inbox/{id}/mark-wechat`
- 预期：返回 `MarkWechatResponse`

### TC-INB-005 模拟数据【P2·API】
- 步骤：`POST /api/dm/inbox/simulate`
- 预期：生成模拟收件

---

## 十八、AI 能力（TC-AI · ai）

### TC-AI-001 AI 设置读写【P1·API】
- 步骤：`GET/PUT /api/ai/settings`
- 预期：配置读写；未配置时 readiness `ai.ok=false`

### TC-AI-002 评论分析【P1·API】
- 步骤：`POST /api/ai/comment-insight` / `/batch`
- 预期：返回洞察（依赖 AI key）

### TC-AI-003 智能拟稿【P1·API】
- 步骤：`POST /api/ai/draft` / `/comment-suggestion`
- 预期：返回话术建议

### TC-AI-004 用户画像分析【P2·API】
- 步骤：`POST /api/ai/profile-analysis`
- 预期：返回画像

### TC-AI-005 通用分析【P1·API】
- 步骤：`POST /api/ai/analyze`
- 预期：返回分析结果

### TC-AI-006 未配置降级【P1·ERR】
- 预期：AI 未配置时相关按钮禁用/提示，不 500

---

## 十九、通知中心（TC-NOT · notifications）

### TC-NOT-001 通知列表【P1·UI】
- 步骤：点铃铛
- 预期：`api-notifications` 拉取列表

### TC-NOT-002 未读计数【P1·UI】
- 预期：红点 `#bell-badge` 与实际未读一致

### TC-NOT-003 标记已读【P1·UI】
- 预期：点击后计数减少

### TC-NOT-004 通知触发【P2·E2E】
- 预期：采集完成/客户成交等事件产生通知

---

## 二十、端到端主流程（TC-E2E）

### TC-E2E-001 登录→采集→获客【P0·E2E】
- 步骤：
  1. `#/accounts` 扫码登录抖音（真人确认）
  2. `#/crawl` 启动采集关键词
  3. `#/comment-growth` 选评论确认回复
  4. `#/customers` 查看新增客户
- 预期：全链路数据贯通；readiness `douyin_login.ok=true`；leads→comments→customers 递进

### TC-E2E-002 客户成交闭环【P0·E2E】
- 步骤：客户推进阶段 → 成交录入 → 数据分析成交数 +1
- 预期：漏斗/成交指标同步

### TC-E2E-003 合规兜底闭环【P0·E2E】
- 步骤：进入安全模式 → 尝试启动采集/发送 → 退出安全模式
- 预期：安全模式下触达被拦；退出后恢复

---

## 二十一、异常 / 边界（TC-ERR）

### TC-ERR-001 接口 404 路由【P1·API】
- 步骤：访问不存在的 `/api/xxx`
- 预期：返回 404 JSON，不 500

### TC-ERR-002 顶层数组陷阱【P1·API】★字段陷阱
- 步骤：`GET /comments/tasks`、`/dm/queue`、`/dm/inbox`
- 预期：返回**顶层数组**；前端 `toArr` 兜底正确解析（非 `{items}`）

### TC-ERR-003 字段命名 snake/camel【P1·API】★字段陷阱
- 步骤：核对 `CrawlAPI`(collected/imported_count)、`api-settings`(r1_enabled)、`commentExecute`(驼峰)
- 预期：未 `_normalize` 的接口返回 snake，前端不重复转义

### TC-ERR-004 评论执行重名响应【P1·API】★已知坑
- 步骤：构造 `match` 命中正常
- 预期：`CommentExecutionResponse(**{**fields,**result})` 不重名（否则 500）

### TC-ERR-005 采集服务 500（无 uv）【P1·API】★已修
- 前置：确认 crawler_manager 用 MediaCrawler `.venv` python
- 步骤：`POST /api/crawler/start`
- 预期：不再 500（历史事件，回归验证）

### TC-ERR-006 地址全不可用拦截【P1·UI】★关键设计
- 步骤：评论发送前所有地址不可用
- 预期：即时 400，不发起发送

### TC-ERR-007 大字段/特殊字符【P2·ERR】
- 步骤：评论含 emoji/超长/HTML
- 预期：入库不截断、展示转义（防 XSS）

### TC-ERR-008 并发重复发送【P2·ERR】
- 步骤：快速重复点「确认回复」
- 预期：幂等/去重，不重复真发

---

## 二十二、通过标准与回归建议

- **P0 全绿**方可发布；P1 失败需评估；P2 记录跟踪
- 每次发布前必跑：TC-ENV（启动）、TC-ACC-007（扫码唤起抖音）、TC-CRAWL-001（未登录跳登录）、TC-CG-002（真发）、TC-RISK（合规）、TC-ERR-002/003（字段陷阱）
- 接口测试建议配 `curl`/Postman 集合，对照本文档 API 用例；UI 用例配截图留痕
- 已知限制：私信发送为 mock（TC-DM-008/TC-INT-005）、AI 需 key（TC-AI-006）、扫码需真人（TC-ACC-007/TC-E2E-001）
